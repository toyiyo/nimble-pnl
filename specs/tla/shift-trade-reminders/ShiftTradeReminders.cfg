\* Planned design: claim the dedupe row (with the status re-check) before the send.
SPECIFICATION Spec
CONSTANTS
    Runs = {"r1", "r2"}
    ClaimFirst = TRUE
INVARIANTS
    TypeOK
    AtMostOneSendPerStage
    NoClaimForClosedTrade
\* A run that finds the trade closed stops. That end state is valid, not a deadlock.
CHECK_DEADLOCK FALSE
