\* EXPECT: violation. Counterfactual: offset paging. A delete between pages shifts the rows, so the loader skips a row that nobody changed.
SPECIFICATION Spec
CONSTANTS
    Keys = {1, 2, 3, 4, 5, 6}
    InitRows = {2, 3, 4, 5}
    PageSize = 2
    MaxWrites = 1
    UseKeyset = FALSE
    Dedupe = FALSE
INVARIANTS
    TypeOK
    NoLostStableRow
