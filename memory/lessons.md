# Lessons Learned

## Category: Domain — Bank Transactions / P&L

### [2026-04-26] Two transfer mechanisms must both be filtered out of P&L
- **Mistake:** Bank transactions assigned a Transfer category (asset/liability/equity-typed chart-of-accounts row, e.g. "Transfer Clearing Account") were rendered as Expenses on the dashboard. The read path filtered only on `is_transfer = false`, but `categorize_bank_transaction` does NOT flip `is_transfer` when assigning an asset-typed category — only the `mark_as_transfer` RPC does. So categorize-then-view was leaking transfers into expense aggregations.
- **Correction:** Added `isTransferCategoryType(account_type)` helper and applied it as a second exclusion (alongside `is_transfer = false`) to every read-path aggregation: `expenseDataFetcher` (transactions, pendingOutflows, splitDetails), `useExpenseHealth`, `useBankTransactions`, and the `Index.tsx` daily-spending filter. Also added pgTAP test pinning that `categorize_bank_transaction` does NOT auto-flip `is_transfer` — if that ever changes, the read-path filter must be revisited.
- **Rule:** A transaction is a transfer if EITHER `is_transfer = true` OR its category's `account_type` is `asset|liability|equity`. Every P&L read path must apply both filters. When adding a new aggregation hook, copy the dual-filter pattern.

---

## Category: TypeScript / React

### [2026-04-26] Widening an interface requires grepping all `as ... []` casts
- **Mistake:** Widened `BankTransaction.chart_account` to include `account_type: string | null`, then updated the projection in the primary `useBankTransactions` hook. Missed a sibling hook `useBankTransactionsWithRelations` that does `as unknown as BankTransaction[]` against a projection that still only selected `account_name`. Typecheck happily passed because `as unknown as` is unsafe by design. CodeRabbit caught it; would have shipped silent `undefined` for any future consumer applying the new transfer-exclusion filter through that hook.
- **Correction:** Added `account_type` to the second projection too. Lesson committed alongside fix.
- **Rule:** When widening an interface used by Supabase row casts, grep `as unknown as <Type>` AND `as <Type>[]` and verify every matching select projection now includes the new fields. Typecheck cannot catch projection drift behind `as unknown as`.

---

## Category: Supabase Edge Functions

### [2026-04-21] Edge function error handling — HTTP codes vs 200 workaround
- **Mistake:** Returned HTTP 200 for all application errors (expired invitation, invalid token) to avoid the Supabase SDK turning them into `error` instead of `data`.
- **Correction:** Return proper 4xx codes (410 expired, 404 invalid, 400 bad request). On the frontend, when `error` is set (from a non-2xx response), read the body with `await error.context.json()` to get the actual error message, then classify from there.
- **Rule:** Always use proper HTTP semantics in edge functions. Handle `FunctionsHttpError.context.json()` in the frontend catch block to extract business-level error messages from 4xx responses.

---

## Category: TypeScript / React

### [2026-04-21] useState setter naming consistency
- **Mistake:** Renamed a state variable but not its setter: `const [authSubmitting, setAuthLoading2] = useState(false)`.
- **Correction:** Rename both: `const [authSubmitting, setAuthSubmitting] = useState(false)`.
- **Rule:** Always rename state variable and setter together. Mismatched pairs compile fine but confuse readers and tools.

---

## Category: Testing

### [2026-04-21] Fake timer cleanup in Vitest
- **Mistake:** Used `vi.useFakeTimers()` in a test file without `afterEach(() => vi.useRealTimers())`, causing fake timers to leak into other test files and produce spurious failures.
- **Correction:** Add `afterEach(() => vi.useRealTimers())` to every `describe` block that calls `vi.useFakeTimers()`.
- **Rule:** Fake timer setup/teardown must be symmetric — always pair `useFakeTimers` with `useRealTimers` in `afterEach`.

---

## Category: Date/Time Calculations

### [2026-04-21] Math.round for day-remaining messages causes off-by-one
- **Mistake:** Used `Math.round(ms / DAY_MS)` to compute "days until expiry". A date 22 hours away rounds to 1 day → "Expires tomorrow" instead of "Expires today".
- **Correction:** Use `Math.floor` for future intervals (ms > 0) and `Math.ceil` for past intervals (ms < 0). Also fix the `days === 0 && ms < 0` case to return "Expired today" rather than "Expired yesterday".
- **Rule:** For UX-facing time-remaining messages, use floor/ceil to avoid premature label advancement. Round numbers belong in math, not calendar copy.

---

## Category: Database Tests (pgTAP)

### [2026-04-21] Hardcoded dates in pgTAP tests cause time-dependent failures
- **Mistake:** pgTAP test `open_shift_claim_timezone.test.sql` used hardcoded dates `2026-04-13` to `2026-04-19`. Once those dates passed, `get_open_shifts` (which filters for future dates) returned NULL and tests 4–5 started failing.
- **Correction:** Replace hardcoded dates with `CURRENT_DATE + (7 - EXTRACT(DOW FROM CURRENT_DATE)::int)` to target the upcoming Sunday. Store computed dates and expected UTC timestamps in a `CREATE TEMP TABLE test_config AS SELECT ...` block; reference them via subqueries in `SELECT is(...)` calls.
- **Rule:** Never use hardcoded future dates in pgTAP tests. Always compute target dates relative to `CURRENT_DATE` so tests remain valid indefinitely.

---

## Category: UI Styling

### [2026-04-21] Direct color tokens in status indicators
- **Mistake:** Used `text-yellow-500` and `text-green-500` for pending/accepted status icons, and `bg-green-100 text-green-600` for the accepted state card.
- **Correction:** Use semantic tokens: `text-primary` for success/accepted, `text-muted-foreground` for neutral states. The amber/yellow warning state can use `bg-amber-500/10 border-amber-500/20` (the AI suggestion panel pattern from CLAUDE.md).
- **Rule:** No direct color classes per CLAUDE.md. Even "status" colors should use semantic tokens or the established amber warning pattern.

---

## Category: React State

### [2026-04-22] Scalar ID vs Set for tracking concurrent async operations
- **Mistake:** Used `resendingId: string | null` to track which invitation row is resending. Replacing the value on each click meant only one row could be in-flight at a time; a second click would clear the first spinner.
- **Correction:** Use `resendingIds: Set<string>` — add on start, delete in `onSettled`. Every row tracks its own state independently.
- **Rule:** Whenever multiple list-row operations can run concurrently, use `Set<id>` not `scalar | null`. The scalar pattern silently drops in-flight state when a second item is activated.

---

## Category: TypeScript

### [2026-04-22] `useState<any>` and `catch (err: any)` hide bugs
- **Mistake:** Used `useState<any>(null)` for invitation details and `catch (err: any)` in validateInvitation — both compile fine but defeat type safety.
- **Correction:** Define a typed interface (`InvitationDetails`) and use `catch (err: unknown)` with an `instanceof Error` guard: `err instanceof Error ? err.message : ''`.
- **Rule:** Never use `any` for component state or catch clauses. `unknown` + type guard is the correct widening strategy.

---

## Category: Security

### [2026-04-22] Leaking raw error messages from 500 responses
- **Mistake:** Edge function returned `error.message` verbatim for unrecognized errors, which could expose internal DB/library details to the client.
- **Correction:** Map known errors to their messages; fall back to a generic `"An unexpected error occurred"` for status 500.
- **Rule:** Only send client-facing messages for errors you explicitly anticipate (4xx). For anything else (5xx), return a generic string and log the real error server-side.

---

## Category: UI Patterns

### [2026-04-22] Card visibility guards that exclude history states
- **Mistake:** The Invitations card condition was `pendingInvites?.some(i => i.status === 'pending' || i.status === 'expired')` — the card disappeared entirely when all invites were cancelled.
- **Correction:** Use `pendingInvites && pendingInvites.length > 0` — show the card whenever any invitations exist (the internal toggle handles cancelled history).
- **Rule:** Card show/hide guards should reflect "does data exist?" not "does active data exist?". Hiding a card because all items are in a terminal state removes context the user might still need.

---

## Category: Development Workflow

### [2026-04-22] Worktree must be created BEFORE brainstorm/plan, not after
- **Mistake:** Ran `/dev` brainstorm + plan phases from the main branch, committing `docs/superpowers/specs/*-design.md` and `docs/superpowers/plans/*-plan.md` directly to `main`. The development-workflow skill's phase order was Brainstorm → Plan → Isolate, which guarantees that every spec and plan commit lands on `main`.
- **Correction:** Always create the feature worktree FIRST, before any artifact is written. Design docs, plans, and code all get authored in the worktree and commit to the feature branch. `main` never receives work-in-progress artifacts for a task.
- **Rule:** `main` is read-only from the workflow's perspective. The very first action after Phase 0 (consult lessons) is Phase 1: Isolate — create a feature branch and worktree. Reordered the development-workflow skill phases accordingly: Phase 0 Consult Lessons → Phase 1 Isolate → Phase 2 Brainstorm → Phase 3 Plan → Phase 4 Build.
- **Recovery pattern when caught mid-flight (corrected 2026-04-22 post-CodeRabbit):** If commits or uncommitted edits have landed on `main`:
  1. `git stash push --include-untracked --message "pre-recover-$(date +%s)"` — preserve uncommitted tracked+untracked edits first (otherwise step 3 destroys them).
  2. `git branch <feature> HEAD && git reset --hard origin/main` — only if there are accidental commits to move; skip if HEAD is already at origin/main.
  3. `git worktree add .claude/worktrees/<feature> <feature>` then `git stash pop` inside the worktree.
- **Why the earlier recovery was wrong:** `git branch <feature> HEAD` only snapshots committed HEAD; `git reset --hard origin/main` then destroys the working tree. The most common case this HARD-GATE catches is uncommitted edits, so skipping the stash defeats the point.

### [2026-05-16] `npx skills add` agent slug is `claude-code`, one per invocation
- **Mistake:** Tried `npx skills add -g -a "Claude Code" -y …` (space-and-caps form, rejected as "Invalid agents") and then `-a claude-code,codex` (comma list, also rejected). Wasted two install rounds on the 10-skill bundle.
- **Correction:** The CLI expects a single lowercase-hyphen slug per `-a`. Valid form: `-a claude-code` or `-a codex` — never quoted, never comma-separated. Install once per agent if you need both sides.
- **Rule:** When a CLI rejects with "Invalid agents/values/etc.," try the lowercase-hyphenated slug form first before trying alternate quoting. Documented bundles should always use the canonical slug.

### [2026-05-16] External CLI dependencies must `::skip::` cleanly, not fail the workflow
- **Mistake (latent):** Author's `/opt/homebrew/bin/codex` was a dangling symlink (Cask install left it pointing at a nonexistent path). A naive runner script that did `codex exec …` would have died mid-pipe and broken the Phase 7a fan-out.
- **Correction:** The Codex adversarial runner checks **both** `command -v codex` AND `codex --version` before doing any work, and emits a `::skip:: <reason>` line + `exit 0` if either fails. The workflow treats adversarial review as best-effort — Claude reviewers still run.
- **Rule:** Any optional external tool the workflow invokes via Bash must (a) detect presence + executability, (b) skip with a structured marker line, (c) exit 0 so the caller can keep going. Never let a missing/broken third-party CLI hard-fail a multi-stage workflow.

---

## Category: Database (PostgREST / Supabase)

### [2026-04-22 → 2026-05-10 UPDATE] PostgREST embeds silently return null when no FK exists — never trust an embed without verifying the foreign key
- **Mistake (original):** Edge function queried `user_restaurants` with `user:auth.users(email)` embed. PostgREST cannot traverse from `public` to the `auth` schema by default — the join returned `null` for every row, no error thrown. Manager emails silently went missing; only employees got notified on time-off requests.
- **Mistake (recurrence on 2026-05-10):** Acting on the original lesson's correction, `send-time-off-notification/buildEmails.ts` was rewritten to embed via `profiles:user_id(email)`. **Same failure mode, new schema:** `public.profiles` in this codebase has zero foreign keys (it stores `user_id uuid` as a plain column, not a FK to `auth.users`). PostgREST has no relationship to follow, so the embed silently returned `null` again. `managersFound` went to 0; managers stopped receiving time-off notifications. Discovered while investigating "did email even fire" during the manager UX redesign — the bug had been latent since the rewrite.
- **Correction:** Replaced both the `auth.users` embed and the `profiles` embed with a deterministic 2-step query: (1) query `user_restaurants` for manager `user_id`s, (2) `select('id, email').in('id', userIds)` against whichever table actually stores the email. No PostgREST relationship traversal at all. Added a regression test that mocks `from()` returning two distinct query builders so the test fails if the code regresses to a single-call embed.
- **Rule:** Before writing a PostgREST embed (`<table>:<fk_column>(...)`), verify the foreign key exists with `\d <table>` or `information_schema.table_constraints`. **Embeds against tables without a declared FK silently return null — they never throw.** When fanning out to recipients, prefer two explicit queries with an `.in('id', ids)` filter; the wire cost of one extra round-trip is negligible compared to silently dropping every row. The previous version of this rule said "always route through `profiles`" — that's a coincidence of one schema, not a rule. The actual rule is: no FK, no embed.

### [2026-04-22] pgTAP fixture flakiness from ON CONFLICT DO NOTHING
- **Mistake:** Test used `INSERT ... ON CONFLICT (id) DO NOTHING` for fixture rows with fixed UUIDs. A stale row from a prior failed run could survive and make the test pass/fail based on prior state rather than the current transaction's inserts.
- **Correction:** Delete-before-insert in FK order inside the same `BEGIN ... ROLLBACK` transaction. Also `ALTER TABLE ... DISABLE ROW LEVEL SECURITY` so fixture inserts don't depend on the caller's role. Keep `ON CONFLICT DO UPDATE` (not `DO NOTHING`) on tables that have auto-create triggers (e.g., `profiles` auto-created from `auth.users`), so the fixture deterministically lands its values regardless of trigger timing.
- **Rule:** Deterministic pgTAP fixtures: (1) RLS off inside the txn, (2) delete-before-insert in FK-safe order, (3) use `ON CONFLICT DO UPDATE` not `DO NOTHING` when a trigger may have pre-created the row.

---

## Category: React Query

### [2026-04-22] `enabled: !!id` is the codebase's "waiting" signal — don't bypass it
- **Mistake:** First draft of a hook early-returned `{ data: 0 }` for an undefined id. Tests and the first UI consumer both passed, but the pattern diverged from every other hook: the rest of the codebase relies on `enabled: !!id` leaving `data: undefined` while waiting, and downstream components check `isLoading` / `data !== undefined` accordingly.
- **Correction:** Use `enabled: !!restaurantId` in the query config. Inside `queryFn`, use the non-null assertion (`restaurantId!`) since the query is gated above. Consumers must then handle `data: undefined` explicitly instead of getting a synthetic `0`.
- **Rule:** Match the codebase's React Query conventions even when your own test works with a shortcut. `enabled: !!id` + `data: undefined` while disabled is the house style; synthetic defaults create asymmetry that downstream components won't expect.

### [2026-04-22] Warning heuristics must guard against error states
- **Mistake:** UI warning computed `(approverCount ?? 0) === 0` to show "No approvers configured". On transient query errors (network, RLS), `data` is undefined with `isLoading: false` — the heuristic treated that as zero and rendered a misleading warning on top of a silent failure.
- **Correction:** Destructure `isError` too and guard `!isError && data !== undefined && data === 0`. Add a unit test for the error path.
- **Rule:** Any UI that derives "empty/zero" from a query result must also check `isError`. React Query returns `data: undefined` for both "still loading (disabled)" and "errored"; conflating either with "genuinely zero" creates confident-looking misinformation.

---

## Category: Testing (React)

### [2026-04-22] Prefer structural (role) assertions over text assertions
- **Mistake:** Tests asserted `queryByText('No approvers configured')` to verify an alert renders. A regression that kept the `role="alert"` div but emptied its text children would pass — the test can't tell the difference between "alert rendered correctly" and "alert rendered as an empty shell".
- **Correction:** Assert `getByRole('alert')` / `queryByRole('alert')` for presence/absence. Keep one belt-and-suspenders text check in the happy-path test; the rest use role only.
- **Rule:** When testing a component's presence/absence, assert on the accessibility role, not the content text. Role assertions catch structural regressions that text-only assertions miss.

### [2026-04-22] Tautological tests that can't fail
- **Mistake:** A test case "chef email is excluded" asserted that a chef's email was not in the approvers list — but the mock data passed to the function never contained a chef to begin with, so the assertion was vacuously true.
- **Correction:** Either include the chef in the mock data (so exclusion is observable) or delete the test. Added the chef to the role filter and asserted it was filtered out.
- **Rule:** Every test should be able to fail if the code regresses. If the test setup doesn't include the thing being tested against, the assertion is tautological and provides no coverage.

---

## Category: Code Review Process

### [2026-04-22] Two-stage review (spec → quality) catches different classes of issue
- **Observation:** The subagent-driven workflow ran two reviewers after each task: spec compliance first, then code quality. Spec review caught "managerCount → managersFound" naming drift from the spec and a tautological chef test (scope issues). Quality review caught `enabled: !!id` convention mismatch and `select('*')` CLAUDE.md violation (craft issues). Neither reviewer caught all the issues alone.
- **Rule:** Keep the two-stage review. Spec review asks "does this match what we agreed to build?" and catches drift; quality review asks "is this the right way to build it?" and catches conventions and smells. They're complementary, not redundant.

### [2026-04-22] CodeRabbit feedback triage: fix, defer, or push back
- **Observation:** CodeRabbit surfaced 7 items on one PR. Triage: 1 real bug (error-state warning), 2 quick quality wins (fixture determinism, test prefixes), 1 plan-doc drift (deferred as historical artifact), 1 markdown lint nit (deferred), 1 style-guide disagreement (pushed back — `bg-amber-500/10` is the documented CLAUDE.md pattern).
- **Rule:** Not every CodeRabbit finding must be addressed. Fix real bugs and quick wins. Defer nits. Push back when the finding contradicts an existing codebase convention — but document the reasoning in a PR comment so future readers know it's intentional, not overlooked.

---

## Category: SonarCloud / Coverage

### [2026-04-25] SonarCloud "new code" coverage ignores vitest excludes
- **Mistake:** Added `src/assets/fonts/micr-e13b.ts` for MICR PDF font registration, but every test file that exercised it (`tests/unit/checkPrinting.test.ts`) used `vi.mock('@/assets/fonts/micr-e13b', ...)` to stub the module. The module showed 0% line coverage in the lcov report. SonarCloud's new-code coverage gate dropped to 67.6% (threshold 80%) and the PR went red — even though `vitest.config.ts` excludes other untested folders.
- **Correction:** Wrote a dedicated `tests/unit/micrPdfChars.test.ts` that imports the real module and tests `MICR_PDF_CHAR_MAP`, `toMicrPdfText`, and `registerMicrFont` directly (stubbing `global.fetch` so the TTF `?url` import resolves under jsdom). Coverage on the file went 0% → 92%, and the SonarCloud gate flipped green on the next push.
- **Rule:** SonarCloud's new-code coverage measures every changed file regardless of vitest's `coverage.exclude` config — excludes only suppress local report noise. If the file is small and 100% mocked elsewhere, write a dedicated direct-import test for it before pushing, or accept that the SonarCloud gate will fail. Mock-only coverage is functionally 0%.

### [2026-04-25] Module-level fetch cache leaks across test cases
- **Mistake:** First draft of `micrPdfChars.test.ts` had two `registerMicrFont` tests — one for the success path (stubbed `fetch` → 200), one for the failure path (stubbed `fetch` → 404). The 404 test always passed-by-accident because the success test had populated a module-scoped `cachedBase64` variable inside `micr-e13b.ts`; the second call short-circuited and never touched the stubbed fetch.
- **Correction:** Removed the 404 test rather than papering over it with `vi.resetModules()`. The success path provides 92% coverage on its own and the cache itself is desired runtime behavior; testing the un-cached failure path requires module-state surgery that has higher maintenance cost than value.
- **Rule:** When a module memoizes async results at module scope (`let cached = null` pattern), tests that depend on running the un-cached path more than once need `vi.resetModules()` + `await import(...)` per case, otherwise the second test silently no-ops. Prefer testing the cache contract once and skipping ambiguous "second-call" tests rather than fighting the memoization.

### [2026-05-16] `sonar.coverage.exclusions` must mirror `vitest.config.ts:coverage.exclude`
- **Mistake:** On PR #500, the SonarCloud Quality Gate flipped red with "Coverage on New Code: 0.0% (required ≥80%)" even though every new behavior had a unit test and every old lesson on Sonar coverage had been followed. Root cause: `vitest.config.ts:coverage.exclude` correctly skipped behavior-less files (route registration in `src/App.tsx`, barrel re-export indexes under `src/components/**/index.ts`, page wrappers under `src/pages/**/*.tsx`, etc.) — but `sonar-project.properties:sonar.coverage.exclusions` did not list those same paths. Sonar therefore included them as "new code" with 0% lcov coverage and the gate collapsed to 0.0%, regardless of how much real coverage the tested files had.
- **Correction:** Aligned `sonar.coverage.exclusions` with `vitest.config.ts:coverage.exclude` in a single commit (`d4d7c716`): added `src/App.tsx`, `src/components/**/index.ts`, `src/contexts/**/*.tsx`, `src/hooks/use*.tsx`, `src/main.tsx`, `src/vite-env.d.ts`, alongside the already-excluded `src/pages/**/*.tsx` and `src/integrations/**`. Added a comment at the top of the Sonar property declaring the alignment expectation so future drift is visible at the diff line.
- **Rule:** `sonar.coverage.exclusions` and `vitest.config.ts:coverage.exclude` are two halves of one config — whenever the vitest excludes change, mirror in Sonar (and vice-versa). Files belonging in BOTH excludes: pure routing wiring (`App.tsx`, route registrars), barrel re-exports (`components/**/index.ts`), page wrappers covered by Playwright E2E, framework boundary files (`main.tsx`, `vite-env.d.ts`). Files belonging in NEITHER (must have real coverage): hooks, utilities, edge-function `_shared/` handlers. The asymmetric case — module excluded from vitest only — is the failure mode: Sonar will count it and report 0%. Different from the 2026-04-25 "module is mocked everywhere" lesson: that one needs a real-import test added; this one is config drift that needs the two property lists re-aligned.

---

## Category: Transactional Ordering (UI Mutations)

### [2026-04-25] Fetch encrypted secrets BEFORE writing audit/state
- **Mistake:** First version of `PrintCheckButton.handlePrint` claimed the next check number, updated the pending outflow to `payment_method: 'check'`, wrote a `printed` audit-log row, and only then fetched the encrypted routing/account secrets needed to render the MICR line. If the secrets fetch failed (network blip, RLS, missing vault entry) the user got an error toast but the audit log already said the check was "printed" — and the next user got a check number that was never used.
- **Correction:** Reordered every print/reprint flow so the secrets fetch (and the precondition checks for missing routing / account_number_last4) runs first. Only after secrets resolve do we claim the check number, mutate the outflow, and write the audit row. Same fix applied to `PrintChecks.tsx` `handlePrint` and `handleReprint` — both had the identical ordering bug.
- **Rule:** In any "fetch sensitive data + write side effects + render artifact" flow, fetch all the inputs first. Side effects (number claims, audit logs, status mutations) only fire after every input has resolved successfully. This way a late-stage failure leaves the system in its starting state instead of a half-printed-half-not state that has to be reconciled by hand.

### [2026-05-10] Pair mutually-exclusive action buttons disable on EITHER mutation, not their own
- **Mistake:** `<TimeOffRow variant="pending">` had Approve and Reject buttons gated by per-mutation `isPending` flags from React Query: `<Button disabled={isApproving} ...>Approve</Button>` and `<Button disabled={isRejecting} ...>Reject</Button>`. CodeRabbit's external pass flagged the race: if a manager double-clicks, or impatiently clicks Reject while Approve is in flight, both mutations fire because each button only watches its own `isPending` flag. The server processes whichever lands second and silently overwrites the first decision.
- **Correction:** Disable both buttons whenever EITHER mutation is in flight: `disabled={isApproving || isRejecting}` on both. The per-mutation flag is still the right shape from React Query — the bug was using only the matching one when the buttons are mutually exclusive choices on the same record.
- **Rule:** When two or more buttons drive mutually-exclusive mutations against the same record (Approve/Reject, Accept/Decline, Pay/Refund), gate every button on the union of all in-flight states, not just its own. The same logic applies to confirm-dialogs that fan out to multiple mutations: lock the entire control group, not the individual button. CodeRabbit's review caught this on a PR I had already self-reviewed and run through a code-quality reviewer subagent — the pattern is subtle enough that an external pass earns its keep.

---

## Category: Testing (Vite/jsdom)

### [2026-04-25] Vite `?url` imports don't resolve in Node — stub fetch instead
- **Mistake:** `src/assets/fonts/micr-e13b.ts` uses `import micrFontUrl from './MICR-E13B.ttf?url'` so Vite emits the asset and gives back a runtime URL. Under vitest+jsdom there's no Vite dev server, so the URL resolves to a path that `fetch()` can't load — tests that exercised `registerMicrFont` failed with `TypeError: Failed to fetch`.
- **Correction:** Stub `global.fetch` per-test with `vi.stubGlobal('fetch', vi.fn(async () => new Response(ttfBytes, { status: 200 })))` and `vi.unstubAllGlobals()` in `afterEach`. Verified the fake bytes flowed through `addFileToVFS` / `addFont` with a base64 string argument matcher.
- **Rule:** Anything imported via Vite's `?url` (or `?raw`, `?inline`) suffix needs runtime stubbing in vitest. Stub `fetch` at the global level with the exact `Response` shape your code consumes; don't try to monkey-patch the import.

---

## Category: Workflow / PR Hygiene

### [2026-04-26] CI green ≠ review comments addressed
- **Mistake:** On PR #479 I notified the user "PR is green and ready for review" the moment `gh pr checks` showed all green. I conflated CodeRabbit's "pass" status (the review-bot's check just means it ran, not that comments are addressed) with "comments handled." When the user asked "did you review the PR comments?" I had to triage 12 real comments — 2 Codex P2 + 10 CodeRabbit — uncovering 2 Major security bugs (PostgreSQL column-level REVOKE bypass via table-level GRANT, cross-restaurant existence probe via distinct error messages), 1 transactional ordering bug (audit row written before secret fetch could fail), and 7 smaller bugs (false-positive regex tests, missing combined `isPending`, leaked secret render, etc.). All of this would have shipped if the user hadn't asked.
- **Correction:** Hardened `.claude/skills/development-workflow.md` Phase 9d into a non-skippable triage gate: must run `dev-tools/refresh-queue.sh --pr <N>`, must `gh api /pulls/<N>/comments` and `/issues/<N>/comments` filtered by author for `coderabbitai|codex|copilot`, and must classify+act on every result before declaring 9e Done. Also added a clarification to Phase 7 that the CodeRabbit CLI run is independent of the GitHub bot — passing one does not satisfy the other. Phase 9e now requires the triage outcome in the user notification ("12 comments: 10 fixed, 2 declined with reply").
- **Rule:** Two independent gates must both be green before claiming a PR is ready: (1) all status checks pass AND (2) every inline / issue comment from a known reviewer (CodeRabbit, Codex, Copilot, human) is either fixed-with-commit or declined-with-reply on the PR. Treat "the bot's check is green" as a signal the bot finished running, not a signal the bot was happy. When in doubt, fetch comments by author and read every one.

---

### [2026-05-01] Bot review claims about "prior version" must be diffed, not believed
- **Mistake:** On PR #484 the Codex bot flagged two P-level issues against my `CREATE OR REPLACE FUNCTION` migration: P1 "the prior function version enforced restaurant membership" (cross-tenant data exposure introduced) and P2 "the tip classification logic was simplified to `LIKE '%tip%'`" (false-positive regression). Both framings asserted what the prior version did. If I had taken them at face value, I would have either (a) added an out-of-scope tenant-guard inconsistent with the rest of the RPC family, or (b) hunted for a "more careful" tip matcher that never existed.
- **Correction:** Read the actual prior migration (`supabase/migrations/20251202100000_aggregate_monthly_metrics.sql`) before responding. The prior function was already `SECURITY DEFINER` granted to `authenticated` with no internal `auth.uid()` / `user_restaurants` guard — same posture as my replacement, and same posture as sibling RPCs `get_pass_through_totals` and `get_revenue_by_account`. The tip-matching CASE expression in my new `monthly_categorized_liabilities` CTE was character-for-character identical to the prior file's. Both findings were based on imagined deltas. Posted a triage reply on the PR explaining this with line numbers from the prior migration so the framing is on record, and filed the (real, pre-existing) tenant-guard concern as a separate-PR-worthy security pass over the whole monthly-metrics RPC family.
- **Rule:** When a bot review says "the prior version did X," `git show <prior-sha>:<path>` (or read the older migration directly) before changing code or accepting the framing. Bots routinely hallucinate the baseline they're comparing against. Two independent things to verify: (1) does the prior file actually contain the property the bot claims, and (2) does my new file actually change it? Disagreement on either is a finding I should reply to, not silently take.

### [2026-05-10] Never commit directly to local `main` — even for docs, monitoring, or "hygiene" work
- **Mistake:** Across 2026-05-09 → 2026-05-10, a sequence of Claude sessions (mostly Opus 4.7, two by Sonnet 4.6) authored a 12-commit UX core-path SLOs series — design doc, plan, Grafana dashboard, Loki recording rules, operator guide, plan/spec corrections — directly on local `main` instead of using `superpowers:using-git-worktrees` + a PR. The pattern probably arose because the work was design docs + monitoring config rather than runtime code, and "PR for a Grafana dashboard JSON" felt heavy. Discovered only because PR #493's `git pull --ff-only origin main` aborted with "diverged: 12 ahead, 5 behind." Without that signal the unreviewed work would have shipped invisibly the next time someone force-pushed main, or stayed dark forever if main got reset elsewhere.
- **Correction:** Rescued the orphaned commits onto a feature branch before any reset: `git branch feature/ux-core-path-slos main && git reset --hard origin/main`. This keeps every SHA reachable so the user can `gh pr create` against the branch and ship the SLO work through the normal review path. Filing this lesson so future sessions catch the violation on themselves, not on a downstream operator.
- **Rule:** The "never commit to local main" rule in `CLAUDE.md` and `.claude/skills/development-workflow.md` is unconditional. It applies to: source code, tests, migrations, edge functions, **docs (specs, plans, lessons, READMEs), monitoring config (Grafana dashboards, Loki rules, alert manifests), `.github/` workflow changes, and CLAUDE.md / skill edits.** If you find yourself about to `git commit` on `main`, stop and run Phase 1 of the development workflow (`git stash` → `git checkout -b feature/<name>` → `git worktree add` → `git stash pop`). Detection signal for future sessions: any time `git status` reports "On branch main" with unstaged work, that is a workflow violation in progress — bail to a feature branch before staging. Recovery signal: `git log origin/main..main --oneline` non-empty after a fetch means past work hit local main directly; rehome to a branch with `git branch <name> main` BEFORE resetting.

### [2026-05-16] `gh pr checks --watch` ≠ Phase 9d done — the 2026-04-26 lesson, repeated
- **Mistake:** On PR #500 I declared "PR is green and ready for human review" the moment `gh pr checks --watch` returned with zero failing jobs. The user pushed back: "*are you subscribed to the PR? there are comments and failures. why did you skip that step? walk me through /dev steps.*" Triage then surfaced 13 unresolved review-bot inline comments (1 P1 security gap in `notify-pin-changed/index.ts` trusting body fields without verifying the caller, 1 P2 behavioral regression — `actor` defaulting to `'manager'` silently fired wrong notifications from `KioskMode.tsx`, 6 Major findings, 5 Minor findings) PLUS a SonarCloud Quality Gate failure (0% new-code coverage from config drift, see the sibling Sonar lesson dated the same day). All of this would have shipped if the user hadn't asked. This is the **same failure mode** the 2026-04-26 "CI green ≠ review comments addressed" lesson already named — and that lesson is in this file. The skip didn't happen because the workflow gate was missing; it happened because I read `gh pr checks --watch` as the whole signal and never opened `gh api repos/.../pulls/N/comments`.
- **Correction:** Walked through Phase 9d explicitly: ran `dev-tools/refresh-queue.sh` to ingest inline comments + SonarCloud findings, mapped each comment to file:line, classified as fix-now / defer / push-back, and shipped two commits — `18773d05` for the 12 source/test fixes, `d4d7c716` for the Sonar config alignment. To prevent the next repeat, the workflow update is mechanical, not behavioral: at Phase 9d entry, write the two literal triage commands into `progress.md` as unchecked `[ ]` items — `gh api repos/$OWNER/$REPO/pulls/$N/comments --jq '[.[] | select(.user.login | test("coderabbitai|codex|copilot|sonarcloud"; "i"))]'` and the SonarCloud dashboard URL — so the checklist physically cannot be ticked complete until each command's output is pasted in. Reading the rule isn't enough if the next step doesn't surface in the working medium.
- **Rule:** `gh pr checks --watch` reports whether check-running jobs **completed**, not whether the bots running them are **happy**. Two independent gates BOTH need to be green before Phase 9d closes: (1) check rollup `[.statusCheckRollup[] | select(.conclusion == "FAILURE")]` empty AND (2) `gh api repos/X/Y/pulls/N/comments` filtered to reviewer authors (`coderabbitai|codex|copilot|sonarcloud`) is either empty or every entry has been `fix-with-commit` or `reply-with-decline`. If gate (2) wasn't actively run, 9d is not done — regardless of how clean (1) looks. This lesson is a literal repeat of 2026-04-26; the failure was reading the prior lesson as advisory rather than as a hard gate. Treat the comment-fetch command and the Sonar dashboard check as required Phase 9d outputs that must appear in `progress.md`, not as items I can mentally promise to remember.

---

## Category: Money / Numeric Math

### [2026-05-01] `Math.round(-0.5)` returns -0 in JS — half-away-from-zero needs sign+abs
- **Mistake:** Initial plan pseudo-code for the cents helper was `Math.round(dollars * 100)`, with a test asserting `toCents(-0.005) === -1`. JavaScript's `Math.round` uses round-half-to-positive-infinity, so `Math.round(-0.5)` returns `-0` (not `-1`) — the test would have failed and any tiny negative half-cent would silently round to zero in production money math.
- **Correction:** Implementation shipped as `Math.sign(dollars) * Math.round(Math.abs(dollars) * 100)` so the rounding is always against a non-negative magnitude, and the sign is reattached afterward. Tests cover both `+0.005 → 1` and `-0.005 → -1`. CodeRabbit later flagged that the plan doc still showed the broken pseudo-code, so I synced the plan to the implementation form with a comment explaining the JS quirk.
- **Rule:** For any money/integer-cents helper, never use `Math.round(value * 100)` directly when the value can be negative. The portable half-away-from-zero pattern is `Math.sign(v) * Math.round(Math.abs(v) * 100)`. Test both signs of half-cent, plus `NaN` and `Infinity`, on the very first commit. If the plan document shows pseudo-code, the pseudo-code must match what the test fixtures actually require — otherwise it is a trap for the next reader.


---

## Category: Time / Timezone

### [2026-05-03] Local-TZ `startOfWeek` makes ISO-week labor wages CI-flaky
- **Mistake:** PR #485 added `calculateActualLaborCostForMonth` with OT-D Hybrid banding — bucket each punch into its ISO week via date-fns `startOfWeek(punchDate, { weekStartsOn })`, run `calculateEmployeePay` over the full week, then distribute pay across days proportional to hours and clip to the calendar month. Pinned the acceptance test wages to `1_282_985` cents from a local PT run. CI (UTC) failed with `1_058_390` — a $2,246 swing from a TZ-dependent week boundary at the month edge. Punches near 2026-03-30 / 2026-05-04 fell into different ISO weeks under PT vs UTC, so the OT bands and the in-month clip both shifted.
- **Correction:** Anchored `monthStart` / `monthEnd` in the test with `new Date(Date.UTC(...))` and re-pinned wages to the UTC value (`1_058_390`). Added a JSDoc comment on the test explaining that the canonical number is TZ=UTC and that production runs in each restaurant's TZ — so cents-level numbers will differ per restaurant. Followed up by filing the underlying production bug for a separate PR: `calculateActualLaborCostForMonth` should bucket weeks in the restaurant's IANA TZ, not the host process TZ.
- **Rule:** Any function that uses `startOfWeek` / `startOfDay` / `format('yyyy-MM-dd')` from date-fns is implicitly host-TZ-dependent. For tests, pin a TZ in the date construction (`Date.UTC` for fixtures, document the assumption). For production, take an explicit IANA TZ argument and use `date-fns-tz` (`utcToZonedTime` / `zonedTimeToUtc`) for the bucketing — never trust the host process TZ to match the user's restaurant TZ. CI runs in UTC and prod servers run in UTC; a green local test on PT means nothing for either.

### [2026-05-03] Toast-style "gross + offset" pass-through rows must filter by allow-list
- **Mistake:** PR #485 also unified Monthly Performance's "POS Collected" so the summary header equals the breakdown panel. The pre-existing `useRevenueBreakdown` and `get_pass_through_totals` RPC summed every row in `unified_sales` whose `adjustment_type` was non-NULL — that included `void`, which Toast writes as a negative offset row in PR #364's "gross + offset" pattern. For Russo's April 2026 the void offsets totalled `-$3,286.25`, which silently inflated POS Collected past Toast's actual deposit total. Tests existed for the totals-by-type math but not for the *which types are counted* contract — so the bug had been latent since the Toast integration shipped.
- **Correction:** Defined an explicit `KNOWN_PASS_THROUGH_TYPES = {'tax', 'tip', 'service_charge', 'discount', 'fee'}` in both `src/hooks/useRevenueBreakdown.tsx` and the SQL function. Both the RPC fast-path and the TS fallback path now drop unknown adjustment types before reducing. Added a `tests/unit/useRevenueBreakdown.passThrough.test.ts` that pins the contract: a fixture containing `void` and `mystery` rows alongside known types must reduce to the same totals as a fixture without them. POS Collected = $92,274.48 by construction (gross + tax + tips + other), matching Toast's deposit report.
- **Rule:** When an upstream integration writes "offset" rows (negative entries to cancel positive ones, like Toast's discount/comp/void offsets), the consuming layer must enumerate which offset types it counts — never `WHERE adjustment_type IS NOT NULL` and assume the upstream universe is closed. Add a contract test asserting the allow-list: a fixture with extra/unknown types passes through unchanged. The same rule applies to chart-of-accounts subtypes, item types, and any other open-ended classifier upstream of money math.

### [2026-05-10] Switching a Date's anchoring convention requires auditing every helper that reads `.getUTC*()` off it
- **Mistake:** PR #491 extended PR #489's `dateOnly` fix to availability + invoicing. Switching `EmployeePortal` from `new Date(exception.date)` (UTC midnight, by spec) to `parseDateOnly(exception.date)` (local midnight) silently broke `src/lib/availabilityTimeUtils.ts`, which extracted the reference date with `referenceDate.getUTCFullYear/getUTCMonth/getUTCDate()`. Before the switch, `new Date("2026-03-08")` was UTC midnight on Mar 8, so UTC accessors returned Mar 8 in any TZ. After the switch, `parseDateOnly("2026-03-08")` is *local* midnight; for users east of UTC (Tokyo, Sydney, most of Asia), that Date's UTC representation is Mar 7 15:00 UTC, so `.getUTCDate()` returned 7 — the helper picked the wrong DST anchor on every transition day (CST instead of CDT on Mar 8, EDT instead of EST on Nov 1). The bug was invisible in CI (UTC) and in the contributor's PT browser (TZ behind UTC, where local and UTC dates still agree at midnight). The existing helper tests used `new Date('2026-XX-15T12:00:00Z')` (noon UTC) fixtures, which can't expose the bug because at noon UTC, local fields and UTC fields agree across most zones. Caught by code-reviewer agent during Phase 5.
- **Correction:** Helpers now read local fields (`.getFullYear/getMonth/getDate`). All callers verified — `parseDateOnly`, `getMondayOfWeek` + day arithmetic, default `new Date()` — produce Dates whose local fields ARE the calendar day the caller is asking about. Added three TZ-portable regression tests using `new Date(year, month, day)` (always produces local midnight on the requested calendar day in any process TZ) anchored on actual DST transitions: Mar 8 (CST→CDT in America/Chicago) and Nov 1 (EDT→EST in America/New_York). Verified the regressions fail under the original UTC-accessor implementation in `TZ=Asia/Tokyo` and pass with the fix; helper suite now passes 20/20 in UTC, PT, and Tokyo.
- **Rule:** When you change the *anchoring convention* of a Date object (UTC midnight → local midnight, or vice versa), every downstream helper that reads `.getUTC*()` off it is suspect — those calls were silently coupled to the old convention. The audit pattern: `grep -rn "getUTC" src/lib/` for any helper consuming a Date you now construct differently, and reason through what the local vs UTC distinction means for that helper's output. For tests of TZ/DST math, prefer `new Date(year, month, day)` over `new Date('YYYY-MM-DDTHH:MM:SSZ')` ISO fixtures: the constructor form pins to local midnight in any process TZ, so a TZ-portable equality assertion ("this Date should anchor DST off March 8 regardless of process TZ") becomes expressible. ISO-string fixtures at noon UTC mask the bug class entirely.

### [2026-05-10] "N days ago" UI text wants calendar-day delta, not `Math.floor(ms / 86_400_000)`
- **Mistake:** First draft of `daysSince(createdAt, now)` for the time-off "requested N days ago" counter used `Math.floor((now - created) / 86_400_000)`. This is a 24-hour delta, not a calendar-day delta. A request created at 23:00 yesterday rendered as "requested today" until 23:00 today — even though every human reader would call that "yesterday's request" all day. Worse, near month/DST boundaries the delta could read 0 or 2 for a request that any user would call "1 day old."
- **Correction:** Compute the delta on UTC-midnight-anchored dates: `Math.floor((utcMidnight(now) - utcMidnight(created)) / 86_400_000)`. Same arithmetic, but both inputs are pinned to 00:00 UTC on their respective calendar days, so the result is the count of full midnights crossed — which is what "N days ago" means colloquially. Tests use a fixed `now` injected via prop for deterministic assertions ("today", "1 day ago", "2 days ago") regardless of the host's wall clock.
- **Rule:** Any UI that says "N days ago / N days from now" wants a calendar-day delta, not a 24-hour delta. Implement it as `floor((utcMidnight(b) - utcMidnight(a)) / 86_400_000)` and inject `now` as a prop/parameter so the component is testable without freezing the system clock. The 24-hour version is almost never what the user means and creates off-by-one bugs that only surface at specific times of day, making them hard to reproduce.

---

## Category: Supabase Edge Functions (continued)

### [2026-05-07] Pure handler in `_shared/`, thin Deno entry — vitest can cover both
- **Mistake:** First draft of the trial-expiry-email and unsubscribe edge functions put all logic in `index.ts` next to `serve(...)`. SonarCloud's "≥80% coverage on new code" gate would have failed: vitest can't load `https://deno.land/std@0.208.0/http/server.ts` or the Deno-specific `Deno.env.get`, so the entire entry file is unreachable from Node tests. We would have had to either skip Sonar (not an option per the gate) or write Deno-runtime tests just for these functions.
- **Correction:** Split each function into two files: `supabase/functions/<name>/index.ts` is the Deno entry (env reads, `createClient`, `Resend`, `serve`); `supabase/functions/_shared/<name>Handler.ts` is the pure logic, takes injected deps as a `Deps` object, and returns a `{status, body}` result. Vitest imports the handler with `import { processX } from '../../supabase/functions/_shared/xHandler'` and exercises every branch with mocked deps. Trial-expiry-emails handler hit 95%+ on the new code; unsubscribeHandler hit 100%. The `index.ts` files stay thin enough that lack-of-coverage there doesn't move the new-code metric.
- **Rule:** For any new Supabase edge function with non-trivial logic, default to the split: `_shared/<name>Handler.ts` exports a pure async function `processX(req, deps): Promise<{status, body}>`; `<name>/index.ts` reads env, builds deps, calls `processX`, serializes the result. Tests live in `tests/unit/<name>Handler.test.ts` and inject `deps` directly. The Deno entry should be small enough to read in one screen — if it grows, more logic belongs in the handler.

### [2026-05-07] `verify_jwt = false` is not "no auth" — it means YOU authenticate
- **Mistake:** Set `verify_jwt = false` in `supabase/config.toml` for `trial-expiry-emails` because pg_cron + pg_net invokes it with the service-role key in the `Authorization` header, not a user JWT. Forgot to add an in-function auth gate. CodeRabbit flagged it as critical on PR #488: any caller could `POST` to the public function URL and trigger bulk email sends. The unsubscribe function legitimately needs `verify_jwt = false` (anonymous users click the link from email), but the cron-only worker did not.
- **Correction:** Added a constant-time `Bearer ${SUPABASE_SERVICE_ROLE_KEY}` check at the top of the `serve` handler (after method check, before any work). Constant-time compare prevents timing oracles on the key. The cron migration already passes `'Authorization', 'Bearer ' || current_setting('app.settings.service_role_key')` so the gate is transparent to the legitimate caller.
- **Rule:** `verify_jwt = false` means Supabase will not check the JWT for you — it does NOT mean the function is open. For every `verify_jwt = false` function, decide explicitly who is allowed to call it. If the answer is "only the cron job / only an internal worker," enforce a service-role Bearer check with a timing-safe compare. If the answer is "anonymous users from a public link," the body itself must contain a verifiable token (HMAC, signed URL, etc.). Never rely on URL obscurity.

### [2026-05-07] Fail fast on required envs — no silent prod fallbacks in preview deploys
- **Mistake:** Initial draft of `trial-expiry-emails/index.ts` had `Deno.env.get('APP_URL') ?? 'https://app.easyshifthq.com'` and `Deno.env.get('TRIAL_EMAIL_FROM') ?? 'EasyShiftHQ <noreply@easyshifthq.com>'`. CodeRabbit flagged it: in a preview/staging deploy where the env wasn't configured, the function would silently use the production URL in unsubscribe links and the production "from" address in send-events. Subscribers would click prod links from staging emails; analytics would record sends as prod traffic.
- **Correction:** Replaced every `?? 'fallback'` with a single fail-fast block: read all required envs into consts at the top, return 500 with `{error: 'Service not configured'}` if any are missing, log which to stderr. Same pattern in the unsubscribe function for `UNSUBSCRIBE_TOKEN_SECRET`. The function is now strictly bound to its deploy environment — preview means preview URLs or it does not run.
- **Rule:** For any required env in an edge function, read it once into a const, fail fast if missing, never default to a hardcoded production value. The `?? 'fallback'` pattern is fine for *optional* configuration (e.g. retry counts, page sizes) — never for URLs, API keys, secrets, or anything that distinguishes environments. If the missing-env message in the logs is "uses prod URL by default," that is a bug, not a feature.

### [2026-05-16] `supabase.functions.invoke()` resolves `{error}` on HTTP failures — it does NOT reject
- **Mistake:** First-pass `useKioskPins` tests mocked the `notify-pin-changed` invoke only with `functionsInvokeMock.mockRejectedValueOnce(new Error('network down'))`. The hook's `onSuccess` treats the notification as fire-and-forget, so I claimed the failure path was tested. A reviewer pointed out that `supabase.functions.invoke()` resolves with `{ data, error }` on HTTP-level failures (4xx/5xx, function crashes, timeout-from-the-gateway) — it only rejects on transport-level failures (DNS, TCP, abort). The `mockRejectedValue` mock alone never exercised the realistic case where the function returns 500 and the SDK packages it into the resolved `error` field instead of throwing. A regression that changed the hook to surface invoke errors to the caller would have silently passed the rejection test.
- **Correction:** Added a second test using `functionsInvokeMock.mockResolvedValueOnce({ data: null, error: { message: 'edge function 500' } })` that asserts (a) `mutateAsync` still resolves, not rejects, and (b) the resolved value still contains the local `pin` + DB record. Now both paths are covered: throw (network/transport) and resolved-with-error (HTTP non-2xx). The hook's `.then(({ error }) => { if (error) console.warn(...) }).catch(...)` chain handles both.
- **Rule:** When mocking `supabase.functions.invoke()` in unit tests, write at least two tests per call site: one with `mockRejectedValue(new Error(...))` for network/transport failures, one with `mockResolvedValue({ data: null, error: { message: '...' } })` for HTTP failures. If a code path is "fire-and-forget, swallow errors," both tests must assert the outer mutation still completes — the error path's existence isn't the assertion, the swallowing is. Cross-reference with the 2026-05-07 split-handler lesson: edge functions ALSO need to return HTTP error JSON (not throw) to keep the resolved-with-error contract honest from both ends.

---

## Category: React Query (continued)

### [2026-05-16] `silent` flag on shared mutation hooks lets bulk callers suppress per-row toasts
- **Mistake:** PR #500 added the `PinRevealDialog` reveal flow for manager-initiated PIN ops. The first iteration of `handleAutoGeneratePins` looped over `mutateAsync({...})` to bulk-generate PINs for every employee without one. Each call triggered `useUpsertEmployeePin`'s `onSuccess`, which fired a "PIN saved" toast. With N missing employees you'd get N toasts stacking under the reveal modal — visually noisy, screen-reader-hostile, and the toast queue could outlive the modal so the user kept dismissing toasts after the operation was done.
- **Correction:** Added `silent?: boolean` to `UpsertPinInput`. The mutation function now returns `{pin, record, silent}`; `onSuccess` wraps the toast in `if (!result.silent) { toast(...) }`. Query invalidation and the `notify-pin-changed` invoke run unconditionally — `silent` only suppresses the user-facing toast, not the side effects. Bulk caller (`handleAutoGeneratePins`) and the single-save handler in `TimePunchesManager` both pass `silent: true`; the reveal modal carries the user feedback for both. Single-employee path in `EmployeePinsCard` (manual reset) and the self-service `/employee/pin` page keep the default `silent: undefined`, so the toast still fires there. Added a "suppresses success toast when silent=true" unit test alongside the existing default-on test.
- **Rule:** When the same mutation hook is shared by single-row and bulk-row callers, give the bulk caller a `silent: true` opt-in to suppress per-call toasts and let it own the consolidated feedback (modal, summary toast, progress bar — whatever fits). Don't infer "bulk vs single" from external signals like array length, route, or auth context; the caller knows its own intent, plumb it explicitly. Side effects (query invalidation, follow-up RPC calls) stay unconditional — `silent` is a UX flag, not a side-effect kill switch.

---

## Category: Security (continued)

### [2026-05-16] Sensitive-secret mutations: explicit `actor` param + post-loop weak-pattern assertion + unmount-safe setState
- **Mistake:** PR #500 lets employees self-serve their kiosk PIN at `/employee/pin`. Three correctness gaps surfaced under code review on commit 54511f2d, each of which would have leaked or weakened the one-time PIN: (1) The `generate()` retry loop ran `while (isSimpleSequence(candidate) && attempts < 6)` and unconditionally fell through afterwards — if all 7 candidates were ascending/descending substrings of `0123456789`, the loop would silently save a known-weak PIN. (2) Both `generate()` and `saveTyped()` called `setRevealed(result.pin)` directly after `await mutateAsync(...)` with no unmount guard; if the user navigated away mid-mutation, the cleartext PIN would land in stale React state. (3) The shared `useUpsertEmployeePin` hook fires `notify-pin-changed` (push + email saying "your PIN was reset") on every mutation — wrong for self-initiated changes, where the employee literally just set the PIN and emailing them about it is noise at best, a credential-rotation signal at worst.
- **Correction:** (1) Bumped retries to 20 (probability of 20 consecutive simple-sequence draws ≈ 10⁻⁵⁷) AND added a post-loop `if (isSimpleSequence(candidate)) { setError('Could not generate a strong PIN. Please try again.'); return; }` — never silently accept the last candidate. (2) Added `isMountedRef = useRef(true)` with a `useEffect` cleanup that flips it false on unmount; both mutation handlers now `if (!isMountedRef.current) return;` between the `await` and the `setState` call. React 18 Strict Mode mount-unmount-remount is safe because the effect resets the ref to true on each fresh mount. (3) Added `actor: 'self' | 'manager'` to `UpsertPinInput`. The hook only invokes `notify-pin-changed` when `actor === 'manager'`; self-service callers pass `'self'` and suppress the notification. Tests assert both actor branches AND the round-trip of the `pin` field (`pin: expect.stringMatching(/^\d{4,6}$/)`), so a regression that sets `pin: undefined` before hashing would fail the test instead of silently saving an empty PIN.
- **Rule:** For any UI flow that displays a one-time secret (PIN, recovery code, generated API key), enforce three discipline points at the boundary: (a) Generator + weak-pattern guard: retry to find a strong candidate, then ASSERT after the loop and fail loudly — never accept the last candidate "as a fallback." (b) Unmount-safe setState: any `setState` that follows `await someMutation()` and would hold a sensitive value must guard against unmount, because React 18 silently swallows the warning and the value otherwise lands on stale state. (c) Explicit caller-intent parameter (`actor`, `source`, `initiator` — pick a name): thread it through the mutation hook so notification side-effects fire only when the right actor caused the change. Don't infer from `auth.uid()` ("if the user is the same as the employee, suppress" is brittle — managers can also be employees). For the test asserting the mutation payload, include the secret field itself in `expect.objectContaining` — `pin: undefined` regressions are exactly the kind of bug that "all green" suites miss.


## Category: Dev-tools / Node module mode

### [2026-05-16] Vitest masks ESM/CJS conflicts in helper scripts shelled by `node`
- **Mistake:** PR #502 added `dev-tools/feedback-log.js` as plain CommonJS (`require`, `module.exports`) and tested it via Vitest, which passed cleanly. Codex caught what local checks did not: `package.json` sets `"type": "module"`, so the moment the `/triage-feedback` slash command ran `node dev-tools/feedback-log.js sanitize`, Node refused to load it (`ReferenceError: require is not defined in ES module scope`). The skill was unrunnable outside the test harness, and 17 green tests masked the regression because Vitest has its own module loader that happily handles CommonJS regardless of repo `type`. Sibling tools in `dev-tools/` (`ingest-feedback.js`, `next-task.js`, `mark-task.js`) are ESM with `import.meta.url === \`file://${process.argv[1]}\`` direct-invocation guards — the new file diverged from the convention without anyone noticing.
- **Correction:** Rewrote the helper as ESM (`import` / `export`, same direct-invocation guard pattern). Verified the CLI end-to-end with a stdin pipe (`echo "..." | node dev-tools/feedback-log.js sanitize`) before re-pushing. Tests still passed unchanged because Vitest accepts both shapes — confirming the gap was Node's loader, not the test code.
- **Rule:** Whenever a `package.json` has `"type": "module"`, any new `.js` file under `dev-tools/` (or any path you intend to invoke via `node <file>`, not just import) MUST be authored as ESM or renamed to `.cjs`. Treat Vitest-green as necessary-but-not-sufficient evidence for shell-invokable helpers — actually run `node path/to/helper.js` once before declaring the helper done. Codex specifically flags these P1s; trust it, and bake `node path/to/script.js --help` (or any harmless arg) into the verification phase for new dev-tools files.

---

## Category: Sanitizers / PII

### [2026-05-16] A truncating sanitizer that appends a marker after slicing violates its own length cap
- **Mistake:** PR #502's `sanitize()` in `dev-tools/feedback-log.js` had the classic shape `if (out.length > MAX_LEN) out = out.slice(0, MAX_LEN) + '… [truncated]'`. The intent was "cap output at 2000 chars" but the actual result was `2000 + suffix.length = 2013` chars. The test was no help — it asserted `expect(out.length).toBeLessThanOrEqual(2000 + suffix.length)`, codifying the bug as the contract. CodeRabbit caught both. For a PII sanitizer that downstream code trusts not to leak beyond a documented ceiling, this is a real boundary violation: a caller that allocates a fixed buffer based on the spec would over-run.
- **Correction:** Changed the slice to budget for the suffix: `out.slice(0, Math.max(0, MAX_LEN - suffix.length)) + suffix`. Test now asserts the strict contract: `expect(out.length).toBeLessThanOrEqual(2000)` AND still confirms `out.endsWith('… [truncated]')`. Added defense-in-depth `Math.max(0, …)` for the degenerate case where `MAX_LEN <= suffix.length`.
- **Rule:** For any truncate-with-marker function, the test must assert `length <= CAP`, NOT `length <= CAP + marker.length`. The latter encodes the off-by-suffix bug into the spec and guarantees future readers won't catch it. When defining the marker as a separate constant, slice against `CAP - MARKER.length` and clamp the budget to `0` so a longer-than-cap marker degrades gracefully instead of producing a negative slice index.

---

## Category: Shell scripting in slash commands

### [2026-05-16] Heredoc JSON construction is unsafe for any field that may contain raw user input
- **Mistake:** PR #502's `/triage-feedback` skill originally built the audit-log row by piping `cat <<EOF { "feedback_text": "<raw value>" ... } EOF` into a helper. CodeRabbit flagged that the moment a reporter wrote `"` or a newline or a backslash in their PostHog survey response, the heredoc would produce malformed JSON and either fail the helper or — worse — silently corrupt the audit log. Heredocs do shell expansion (`$VAR`, `` ` ``) but NOT JSON escaping; quoting via `"..."` doesn't help when the *value* itself contains a `"`.
- **Correction:** Rewrote the LOG step to use `jq -n --arg key "$value" ... '{key: $key, ...}'`. `jq` properly JSON-escapes every `--arg`, including quotes, backslashes, and embedded newlines. The pattern: build the row as a JSON string into a shell variable, then pass that variable to the helper — never inline raw text into a heredoc that pretends to be JSON.
- **Rule:** Whenever a shell-driven command writes JSON containing fields that originated from external systems (survey responses, log lines, user names, error messages), use `jq -n --arg` (or `python -c 'import json,sys; print(json.dumps(...))'` if `jq` isn't available) to build the JSON. Reserve heredocs for *static templates* — the moment any field is `$VAR` derived from external input, you must JSON-escape. The same rule applies to YAML, SQL, HTML, and any other quote-aware format embedded in a shell pipeline.

---

## Category: Supabase Edge Functions (continued)

### [2026-05-17] Per-attempt timeout ≠ bounded total time in a multi-model fallback loop
- **Mistake:** PR #506's `generate-schedule` edge function chains 5 OpenRouter models with `callModel(..., AbortSignal.timeout(30_000))` per attempt. The performance reviewer flagged that if 4 models stall to the 30s timeout and the 5th finally responds, total elapsed is ~120s. Supabase edge functions hard-kill at ~150s of wall clock, so a marginal slow chain plus a slow final model could OOM-kill the response before any diagnostic JSON reached the client — i.e. the user sees the exact "silent empty schedule" symptom Bug 8 was supposed to eliminate. Per-call `AbortSignal.timeout()` bounds individual fetches, not the iteration count × timeout product.
- **Correction:** Added a wall-clock budget guard above the model loop: `const modelLoopStart = Date.now(); const MODEL_LOOP_BUDGET_MS = 90_000;` and `if (Date.now() - modelLoopStart > MODEL_LOOP_BUDGET_MS) break;` at the top of each iteration. 90s leaves ~60s headroom for response serialization, validator pass, and the 422 path's structured payload. Logs which model index the budget tripped on so on-call can see "models 1-3 all stalled" patterns. The break falls through to the existing `if (!aiResult)` 502 handler, so the client still gets a proper error response instead of the gateway timeout.
- **Rule:** Whenever a function iterates over N retries/fallbacks with per-attempt `AbortSignal.timeout(T)`, compute the worst-case `N × T` and compare to the platform's wall-clock limit. If `N × T ≥ limit - response_overhead`, add an explicit total-budget guard at the loop head: capture `start = Date.now()`, check `elapsed > BUDGET` before each iteration, break early. The guard ensures the function *always* returns a structured response (success, partial, or error) within the platform window, instead of getting killed mid-attempt and surfacing as a 504 to the caller. Default headroom: leave 30-60s for serialization and any post-loop work on platforms with a ~150s edge limit.

### [2026-05-17] "Server-debug only" fields on shared types leak through hand-mapped response builders
- **Mistake:** PR #506's `schedule-validator.ts` defined `DroppedShift = { code, day, position, start_time, end_time, message }` with a JSDoc warning "`message` retains UUIDs for server logs; never include it verbatim in client-facing responses." The 422 error path correctly mapped only the safe fields. The 200 success path's `dropped_reasons` array was added later and built by iterating the same `dropped` array — but the developer who wrote it shallow-copied the whole object, so UUIDs leaked into the response visible to any signed-in tenant. The leak was caught mid-Phase-7a security review (commit 8424f4f2) and would have shipped without it because the type signature happily accepted the full object.
- **Correction:** Refactored both response builders to use the same UUID-free projection (DropCode enum + safe fields), and added a JSDoc-enforced narrower type `ClientSafeDroppedShift = Omit<DroppedShift, 'message'>` so future "shallow copy the validator output" mistakes fail typecheck instead of leaking. The validator's `message` field stays for server logs; consumers must explicitly opt into seeing it.
- **Rule:** When a shared type contains a field that's safe for some consumers (server logs, debug telemetry) but unsafe for others (client responses, public APIs, support tickets), define a narrower `ClientSafeX = Omit<X, 'sensitiveField'>` and force unsafe-side callers to project through it. A JSDoc warning on the field is necessary documentation but insufficient enforcement — the next dev who adds a response path will iterate the array, shallow-copy, and ship the leak. The compiler can prevent this if you make the unsafe field unreachable in the typed surface area.

---

## Category: Dev workflow / Worktree hygiene

### [2026-05-17] Worktrees need their own `npm install` — Vitest's parent resolution masks the gap
- **Mistake:** PR #506's worktree at `.claude/worktrees/ai-scheduler-fill-fix/` ran `npm test --run` cleanly through Phases 4-7 (3937 tests passed). Phase 8's `npm run build` then failed with `Cannot find module vite/bin/vite.js`. The worktree's `node_modules/` was empty; Vitest had been silently resolving its dependencies via Node's parent-directory `node_modules` lookup (it walks up to the parent repo's `node_modules`), but Vite's binary lookup uses `npm`'s `node_modules/.bin/vite` from the *current* directory and doesn't walk up. Result: tests green for 8 phases, build red 60 seconds before PR push.
- **Correction:** Ran `npm install --no-audit --no-fund` in the worktree (942 packages); build then succeeded in 16s. Added a Phase 1 (Isolate) step to the personal workflow checklist: after `git worktree add`, immediately run `npm install` even if tests appear to work — they're lying. The worktree is a separate npm root despite sharing a `.git` directory with the parent.
- **Rule:** Every new git worktree needs its own `npm install` (or `pnpm install`, `yarn install`) before any work begins, even if tests pass without it. Test runners like Vitest/Jest resolve through Node's full parent-`node_modules` walk and will silently use the parent repo's deps; build tools and bundlers like Vite/Webpack/Rollup look at `./node_modules/.bin/` first and will fail with cryptic "module not found" errors when the worktree's bin dir is empty. If your isolation skill creates worktrees, make `npm install` part of the Isolate phase, not a discovery during Build.

---

## Category: Code review / Multi-model review hygiene

### [2026-05-17] Triage reviewer findings by writing the regression test first; theoretical → document & skip
- **Mistake:** Phase 7a multi-model review flagged 8 findings across 4 reviewers (sound-logic, security, performance, maintainability). Two of them (sound-logic MEDIUM on `dateForDayOfWeek` local-tz Date math, sound-logic MEDIUM on `convertOne` end_time `"00:00:00"` edge case) read as serious but were rooted in a misreading of how `new Date(year, month, day)` interacts with timezone math and how the UTC short-circuit in `convertOne` handles midnight ends. First instinct was to "just fix them to be safe" — which would have added defensive code for scenarios that don't occur, polluted the diff, and risked introducing real bugs while neutralizing imaginary ones.
- **Correction:** For each flagged edge case, wrote the smallest failing-or-passing regression test BEFORE deciding whether to change production code. The two flagged tests both passed against existing code (proving the reviewer's concerns were theoretical given realistic inputs: UTC tz never reaches the split branch, and non-UTC tz conversion math never produces a localEnd of exactly `"00:00:00"`). Kept the tests as positive proof and documented in the commit message which findings were skipped and why — so future reviewers (and Codex on the next PR) won't re-flag the same lines.
- **Rule:** Multi-model code review will surface a mix of real bugs, real-but-low-severity nits, and theoretical edge cases. Before "fixing" anything beyond P0 security/correctness, write the regression test that would fail if the bug were real. If the test passes against current code, the finding is theoretical: don't change production code, but keep the test as positive proof of the invariant and write a sentence in the commit message naming the finding and explaining why no change was made. This prevents (a) defensive-coding diff bloat, (b) re-litigating the same nit on every subsequent review, and (c) the next AI reviewer flagging the line again because it "looks suspicious." The commit message becomes the authoritative answer to "didn't a reviewer flag this?"

## Category: Testing (React) (continued)

### [2026-05-17] Source-text regression tests beat mocking 30+ hooks for class-name guards
- **Mistake:** PR #504's `POSSales.tsx` mounts a page that pulls in ~30 hooks (auth, restaurant context, products, sales, AI suggestions, virtualizer, dialogs, etc.). The first attempt at a regression test for "outer wrapper + header row use `sm:` not `md:`" tried to render the page and assert classes on the rendered DOM. That required mocking every hook the page touches — a non-starter for a UI-only mobile fix that nobody wanted to babysit through future hook additions. The test was about to bloat from ~10 lines of intent into 200+ lines of irrelevant scaffolding, and the next person to add a hook would break it.
- **Correction:** Wrote a *source-text* test instead: `fs.readFileSync(POSSALES_TSX_PATH, 'utf8')` then word-boundary regex on the file content. Positive assertions for the new classes (`sm:flex-row`, `sm:items-center`, `sm:justify-between`, `sm:space-y-10`), AND **negative** assertions that the old breakpoint forms (`md:flex-row`, `md:space-y-10`) do NOT appear. A regression that adds `md:flex-row` anywhere in the file would fail. Zero hooks mocked, zero React rendering, runs in <50ms. Same pattern reused for the `h1` size-scale assertion in Phase 7b.
- **Rule:** When the *intent* of a test is "this specific string of CSS classes lives in this specific file" — not "the rendered component behaves correctly" — read the source as text and grep it. Always pair the positive assertion (new class present) with a negative assertion (old class absent), because positive-only tests pass when both old and new exist side by side. Use word-boundary regex (`/\bmd:flex-row\b/`) to avoid matching `lg:md:flex-row` or `sm:md:flex-row` false positives. Reserve full-component render tests for behavior; reserve source-text tests for static class-name policies and lint-style guards. The trade-off is that source-text tests are blind to runtime class composition (cn(), clsx, conditionals) — use them only when the class lives as a literal in the JSX.

### [2026-05-17] `as unknown as T` double-assertion in test data silently shields missing required fields
- **Mistake:** PR #504's first cut of `SplitSaleView.mobile.test.tsx` built child split records with `{ id: 'child-1', itemName: 'Burger', totalPrice: 15 } as unknown as UnifiedSaleItem`. The double assertion (`as unknown as T`) bypassed every TypeScript check, so the tests happily compiled even though `UnifiedSaleItem` requires `restaurantId`, `posSystem`, `externalOrderId`, `quantity`, `saleDate`, `syncedAt`, `createdAt`. The tests still passed because `SplitSaleView` happens not to read those fields — but the moment the component starts using `restaurantId` (e.g., for a "view all sales by this restaurant" link), the test stays green while the production code crashes on `undefined`. CodeRabbit flagged this as a Major; the local CodeRabbit CLI's second iteration came back clean only after the fix.
- **Correction:** Replaced every `as unknown as UnifiedSaleItem` with a `makeSale(overrides: Partial<UnifiedSaleItem>): UnifiedSaleItem` factory that returns a fully-populated object with every required field set to a sensible default, then spreads the per-test overrides on top. Tests now read `makeSale({ id: 'child-1', itemName: 'Burger', totalPrice: 15 })` — same intent, full type safety. A future required-field addition on `UnifiedSaleItem` will surface in the factory in one place, not scatter across 5 ad-hoc cast sites.
- **Rule:** `as unknown as T` is a code smell anywhere it appears, but it's *especially* dangerous in test fixtures because it disables the one check that would catch shape drift between production types and test data. Forbid double assertions in tests by convention: if the type has 3+ required fields, write a `makeX(overrides: Partial<X>): X` factory. If you genuinely cannot satisfy the type (e.g., mocking a third-party SDK with private symbols), use `satisfies Partial<T>` + a single `as T` with a code comment explaining what's elided. Never `as unknown as T` in tests.

---

## Category: Code Review Process (continued)

### [2026-05-17] Multi-model reviewers can claim "in your diff" for context lines — verify via hunk headers
- **Mistake:** Phase 7a's performance reviewer flagged "2 Major inline-arrow re-creations in POSSales.tsx — `formatCurrency={(n) => …}` and `onSplit={(s) => …}` at lines 1238–1239, in the lines this PR modifies." Both `formatCurrency` and `onSplit` are passed to a memoized child, so the inline arrows defeat the memoization on every render. The finding *looked* like a real perf bug AND looked like it was in scope. It was a real perf bug; it was NOT in scope. `git diff main...HEAD -- src/pages/POSSales.tsx` showed lines 1238–1239 as ` ` (space-prefixed context lines), not `+` or `-`. The reviewer mapped its "lines I see in the file" to "lines you changed," which is wrong — hunks include 3 lines of context on either side of every change. Acting on this would have ballooned PR #504's scope from a 14-file mobile-UI fix to a render-hot-path refactor with its own design implications.
- **Correction:** Walked the hunk headers (`@@ -1234,12 +1234,12 @@`) and confirmed lines 1238–1239 were unchanged context. Deferred the perf findings to a separate ticket with a note linking the reviewer's exact framing. Same Phase 7a maintainability reviewer made a comparable mistake on a `title="Create rule"` attribute that was redundant given a visible text label — its own analysis admitted "high ambiguity for the next developer" while marking the issue Major. Removed the attribute (it was redundant) but logged the reviewer's confidence/severity mismatch.
- **Rule:** Multi-model reviewers tend to conflate "in the file" with "in the diff." When a reviewer cites a line number and severity, verify against `git diff main...HEAD -- <file>` (or look at the hunk header offsets) before acting. If the line is a context line, the finding may still be real but it is **scope creep** — file a follow-up issue or defer to retrospective, do not silently expand the PR. Also: when the reviewer's own confidence language ("high ambiguity for the next developer," "low ambiguity for screen readers") undercuts the severity it assigned, downgrade. Reviewers are advisors, not adjudicators.

---

## Category: Workflow / PR Hygiene (continued)

### [2026-05-17] E2E flake recognition: cross-check the failing spec against recent main-branch runs before re-running
- **Mistake:** PR #504's E2E Shard 4 hard-failed on `tests/e2e/tips-flow.spec.ts:36 — manual tips split by hours with live preview`. The naive reaction is either (a) re-run blindly ("E2E is always flaky") or (b) panic ("my PR broke tips?"). Both are wrong. The first masks real regressions if the failure is yours; the second wastes time fixing nothing.
- **Correction:** Three-step triage before re-running. (1) `git diff --name-only main...HEAD | grep -i <feature>` to confirm the failing feature has zero code in the diff — PR #504 touched POSSales/SaleCard/SplitSaleView/BulkActionBar, zero tips files. (2) Pull recent main-branch runs of the same workflow: `gh run list --branch main --workflow "Unit Tests" --limit 6` then for each, `gh run view <id> --json jobs | jq '.jobs[] | select(.name | test("E2E Tests")) | "\(.name): \(.conclusion)"'`. PR #500's main run (b1397c55) hard-failed Shard 4 on `tip-sharing.spec.ts:92:53` with the same `getByText('$NNN.NN').first() not visible` live-preview pattern. (3) Same root cause + zero diff overlap = pre-existing flake. Safe to `gh run rerun <id> --failed`. Document the cross-check in `progress.md` so the next reviewer doesn't have to redo it.
- **Rule:** Never re-run a CI failure without first proving it's unrelated to your diff and not novel. The proof is two grep-style commands: scope (`git diff --name-only main...HEAD | grep <feature>`) and history (`gh run list --branch main` + filter for matching failures). If both come back "zero diff, prior failure on main," it's a flake — re-run and log it. If diff has the feature, fix it. If diff is clean but no prior main failures, treat as novel and investigate (likely an environmental drift, not a flake). The `getByText('$NNN.NN').first()` Playwright pattern against React live-preview tends to flake in particular because the preview re-renders during keystroke ingestion and the DOM mutates faster than Playwright's auto-wait can settle — long-standing pattern, separate fix.

### [2026-05-17] `gh pr checks --watch` drops on long CI runs; use `gh run view --json` in an until-loop instead
- **Mistake:** Twice in PR #504's ship phase, `gh pr checks 504 --watch --interval 30` died after several minutes with `Post "https://api.github.com/graphql": read tcp ...->...:443: read: connection reset by peer`. Each drop required re-checking state and restarting the watch, wasting cycle time and burning the same harness budget. The watch subcommand uses a long-lived GraphQL subscription that NAT/firewall middleboxes happily reset after a few minutes of inactivity-from-their-perspective; not a `gh` bug, just a fragile transport.
- **Correction:** Replaced the watch with a Bash until-loop that uses single-shot REST calls: `until [ "$(gh run view <id> --json status | jq -r .status)" = "completed" ]; do sleep 30; done`. Each call is a fresh TCP connection that can't drop mid-poll. Wrapped in `run_in_background: true` so the harness notifies on loop exit. Ran for 16 minutes uninterrupted across the E2E shard window where `--watch` had failed twice in a row.
- **Rule:** For any CI wait inside an agentic workflow, prefer single-shot polling over streaming `--watch` subcommands. The until-loop pattern (`until <single-shot-check>; do sleep N; done`) survives connection drops, is trivially backgroundable, and lets you log interim state if you want. Reserve `--watch` for interactive terminal use where you'll notice if it drops. Same advice applies to `kubectl logs -f`, `tail -F` over SSH, and any other streaming subscription a long-running automation might depend on.

---

## Category: UI Patterns (continued)

### [2026-05-17] When fixing responsive UI, pick ONE breakpoint and use it consistently — don't mix `sm:`/`md:`/`lg:` ad hoc
- **Mistake:** Before PR #504, `POSSales.tsx` mixed breakpoint families across adjacent responsive utilities: outer wrapper used `space-y-8 md:space-y-10`, the immediate child header used `md:flex-row md:items-center`, the BulkActionBar inside used `max-w-[95vw] md:max-w-4xl w-full md:w-auto`, and the h1 used `text-[2rem] md:text-[2.5rem]`. All targeted "tablet+" intent but at the 768px `md:` threshold — leaving the 640-768px viewport range (large phones in landscape, small tablets in portrait) stuck in the cramped "mobile" layout even though there was room to expand. The frontend-design-reviewer caught this as a Major breakpoint-consistency issue across multiple commits; one straggler (the h1 size scale) slipped through the Phase 5 sweep and had to be caught in Phase 7b.
- **Correction:** Adopted a "single breakpoint policy" for the whole feature: every responsive switch on POSSales/SaleCard/SplitSaleView/BulkActionBar moved to `sm:` (640px). This expanded the layout earlier (better use of the 640-768px range) and made the file scannable — every `sm:` in the diff means the same thing. Added negative source-text assertions (no `md:flex-row`, no `md:space-y-10`, no `md:max-w-4xl`) to lock the policy against regression. The Phase 7b miss taught us to grep the file for *every* responsive utility at the chosen old breakpoint, not just the ones the reviewer cited.
- **Rule:** When a feature spans more than a couple of responsive-class call sites, choose one Tailwind breakpoint as the "mobile/desktop pivot" for the whole feature (usually `sm:` for content-dense pages, `md:` for sidebar/grid pages) and stick to it. Mixed breakpoints look fine in isolation per file but create dead zones in the viewport-width spectrum where the layout transitions don't line up — users see the AI card go full-width at 640px but the title size still small until 768px, etc. After a breakpoint sweep, **grep the entire file** for `[a-z]+:` Tailwind variant prefixes before declaring done; it's the only reliable way to catch the one straggler your eyes will skip past. Pair every breakpoint migration with a negative source-text test asserting the old prefix is gone.

## Category: Code Review Process (continued)

### [2026-05-18] When CodeRabbit prescribes server-side gating, ask if it would break the contract — surface in the client instead
- **Mistake:** PR #506's `generate-schedule` returns 200 with the partial-fill schedule when `validShifts.length > 0 && validShifts.length < totalRequiredSlots`. CodeRabbit suggested widening the 422 zero-shift guardrail to also fire on underfill (`validShifts.length < totalRequiredSlots`). On the face of it, this surfaces the underfill — but it also means a manager who got 18 of 30 slots filled would now get an error toast and zero shifts inserted, when they actually wanted those 18 saved as a starting point. The CR suggestion was technically a correctness fix for "user sees stale-looking schedule" but a regression for "user wants partial result."
- **Correction:** Kept the 200 success path intact for partial fills. Moved the underfill visibility into the client toast: `Filled X of Y required slots` when `data.shifts.length < data.metadata.total_required_slots`, plain `N shifts created` otherwise. Added a parallel enrichment to the 422 destructive toast: `Filled 0 of Y required slots (AI proposed M, all dropped). Top reason: CODE (count).` — distinguishes "AI returned nothing" from "AI returned plenty, all invalid." Tests assert both branches and the negative case (omit "AI proposed" when generated=0).
- **Rule:** When an automated reviewer prescribes "tighten the server-side guard" for what is really an observability problem, walk the contract: who calls the endpoint, what do they do with the existing response shape, what breaks if a 200 becomes a 422? If the CR's fix is technically correct but would silently break a partial-success contract, do the fix's *intent* (make the underfill visible) in a layer that doesn't change the contract (client toast, dashboard banner, return-shape metadata). Capture the decision in the commit message — `dropped CR #N (server 422 on underfill) — would break partial-fill UX; surfaced in client toast instead.` The next reviewer (CR or human) seeing the same lines won't re-flag it.

### [2026-05-18] Day-aware overlap needs absolute-time math, not a wider "day predicate"
- **Mistake:** PR #506's `schedule-validator.ts` filtered double-booking candidates with `v.day === shift.day && shiftsOverlap(v, shift)`. CodeRabbit pointed out an overnight shift on day N (22:00→02:00) could collide with a morning shift on day N+1 (00:00→06:00) but be filtered out by the day predicate. First instinct: widen the day predicate to `|v.day - shift.day| <= 1`. That breaks immediately: two normal non-overnight shifts on consecutive days (e.g. Mon 10-18 and Tue 10-18) would call `shiftsOverlap`, which has no day context, sees overlapping minute ranges, and returns true → false-positive double-booking on every consecutive-day shift.
- **Correction:** Added `shiftsConflict(a, b)` that delegates to `shiftsOverlap` only for same-day pairs and does absolute-time math for cross-day pairs: parse both `YYYY-MM-DD` days, compute integer day offset, project both shifts into a common minutes-from-A's-midnight frame (extending `end_time` by 1440 when `end <= start`), then check `aStart < bEnd && bStart < aEnd`. Added a guard `if (!aOvernight && !bOvernight) return false` so consecutive-day normal shifts short-circuit. Tests cover the four corners: overnight + next-day morning collision (true), overnight + next-day late morning no-overlap (false), two consecutive normal day shifts (false), two consecutive overnight shifts (false).
- **Rule:** "Same calendar day" and "same operational night" are two different reference frames. Helpers that take only `(start_time, end_time)` with no day context can only reason within one frame and will silently emit false positives/negatives at the boundary. For schedule logic that may span midnight, do the math in absolute minutes from a chosen reference (epoch ms or minutes-from-day-N-midnight) and only project back to local time at display boundaries. Don't paper over the missing context by widening the upstream filter — you'll trade one missed bug for a more common false alarm.

---

## Category: Schema → Consumer Contract Drift

### [2026-05-22] A schema field is only as useful as the LAST consumer that reads it
- **Mistake:** PR #511 traced four bugs in `generate-schedule`, three of which had the same shape: a field existed at the schema layer but a downstream consumer dropped or ignored it. `shift_templates.capacity` was a real column (DB CHECK `>= 1`), surfaced in the templates table, used in the planner UI — but `computeRequiredStaff` used `fromMinCrew ?? fromPattern ?? 1` and never read it, so restaurants without `staffing_settings` collapsed every (template, day) to 1. Parallel pattern: the LLM dutifully returned `template_id` on every shift, the response schema typed it as required, but `useGenerateSchedule.shiftsToInsert` mapped fields one by one and omitted `shift_template_id` — every AI shift landed with `shift_template_id IS NULL`. Twin to that: `template.days` was already in the prompt and the LLM's input, but `validateGeneratedShifts` never checked `template.days.includes(getDayOfWeek(shift.day))`, so a weekend-only template assigned on Monday would pass position/availability/window/dedup and persist.
- **Correction:** Three fallback-chain fixes plus one new validator step. Capacity: `fromMinCrew ?? fromPattern ?? tpl.capacity ?? 1`. Persistence: added `shift_template_id: shift.template_id?.trim() || null` to the insert payload and widened the type to `string | null | undefined` to absorb older bundles or strict-schema fallbacks that omit it. Validator: new `DAY_NOT_IN_TEMPLATE` DropCode + step 4 check after UNKNOWN_TEMPLATE. Whenever you add a check, also extend the diagnostic-summary droppedReasons mapping so users see "Template not active on YYYY-MM-DD" instead of an unmapped UPPER_SNAKE code.
- **Rule:** A schema field is a contract with every code path that touches the row. Before claiming a feature is "wired up," walk it end-to-end: schema → SELECT in the edge function → mapper into typed context → LLM input/output → validator → persistence → query/planner read path. Each hop is a chance to silently drop the field. The easiest way to find these is to grep the field name across both `supabase/` and `src/` and verify it appears in every layer; absence on any layer is the bug.

---

## Category: Validator Context Shape

### [2026-05-22] When a Set's id is the only thing a check needs, leave it; when the check needs richer per-id data, refactor to Map
- **Mistake:** First draft of the day-of-week validator check kept `ValidationContext.templateIds: Set<string>` (used for the UNKNOWN_TEMPLATE step) and added a parallel `templateDays: Map<string, number[]>` sibling. Two structures, two `has`/`get` calls, two places to keep in sync if a template id ever changes mid-call. The frontend design review flagged it before code: "you're now using ids to look up a template — make it a Map keyed by id, derive `templateIds` semantics from `templates.has(id)`."
- **Correction:** Refactored to `templates: Map<string, { days: number[] }>`. Step 3 (UNKNOWN_TEMPLATE) uses `templates.get(id)` and drops on falsy. Step 4 (DAY_NOT_IN_TEMPLATE) uses the same `template` variable. One structure, one lookup, both checks share state. Renumbered validator steps from `3 → 3b → 4 → 5 → 6 → 7` to `1 → 2 → 3 → 4 → 5 → 6 → 7 → 8` while at it — the "3b" came from a prior incremental add and was already drift.
- **Rule:** If a validator step needs to read additional per-id data beyond existence, don't bolt on a sibling Map — promote the existence-only Set to a Map whose value carries that data. Existence checks derive trivially from `map.has(id)`. Sibling structures invite drift: a future migration that updates `templates` but forgets `templateDays` will silently downgrade the day-of-week check to "always passes." Promote first, don't pair.

---

## Category: Defense-in-Depth After Adding a Constraint

### [2026-05-22] A new DB CHECK constrains future writes — pre-existing rows can still poison the runtime
- **Mistake:** PR #511 introduced `shift_templates.capacity NOT NULL DEFAULT 1 CHECK (capacity >= 1)` (in a prior migration on main) and updated `computeRequiredStaff` to read `tpl.capacity` directly. Sound-logic reviewer flagged a Major: the CHECK protects new INSERT/UPDATE but says nothing about rows written before the constraint was added. If migration drift produced a `capacity = 0` or `capacity = NaN` row, the new fallback `fromMinCrew ?? fromPattern ?? tpl.capacity ?? 1` would resolve to 0 or NaN — *worse* than the bug we just fixed, because NaN propagates silently through later math.
- **Correction:** Added a `capacityFloor` guard at the read site: `const capacityFloor = typeof tpl.capacity === "number" && tpl.capacity >= 1 ? tpl.capacity : 1; ... fromMinCrew ?? fromPattern ?? capacityFloor`. Added two unit tests pinning the behavior for `capacity = 0` and `capacity = NaN` → both fall back to 1. JSDoc on the interface field documents the DB enforcement: `/** Manager-stated headcount required per (template, day). The DB enforces \`DEFAULT 1 CHECK (capacity >= 1)\`. */`.
- **Rule:** Defense-in-depth has two layers when you tighten a schema: the constraint protects writes from now on; a read-site guard protects against pre-existing malformed rows. Always add both, and pin both with tests. Comment the read-site guard with the DB-enforced invariant so the next reader doesn't see it as dead code and rip it out. The cost is one extra typeof check per row; the savings are silent NaN-poisoning of every downstream metric.

---

## Category: Workflow Discipline

### [2026-05-22] Green CI is not Done — comment triage is a separate, mandatory step (the 2026-04-26 / 2026-05-16 lesson, repeated a third time)
- **Mistake:** On PR #511 I watched `gh pr checks` go green (17 pass / 0 fail / 2 skipping) and declared the PR "ready to merge." I did not run `dev-tools/refresh-queue.sh`, did not run the direct `gh api .../comments` and `gh pr view --json reviews` queries, and did not read the CodeRabbit summary review on the PR. The user pushed back: "it is not enough to have it green and not look into codex, coderabbit, and human comments." Re-fetching surfaced an actionable CodeRabbit finding — `t.capacity ?? 1` in `generate-schedule/index.ts:268` leaves 0/NaN intact and the prompt would disagree with the staffing floor. **This is the third recorded instance of the same failure mode** — see [2026-04-26] "CI green ≠ review comments addressed" (line 234, PR #479, 2 Major security bugs hidden in unread comments) and [2026-05-16] "`gh pr checks --watch` ≠ Phase 9d done — the 2026-04-26 lesson, repeated" (line 251, PR #500, 1 P1 + 1 P2 hidden). The workflow had a Phase 9d "Review-Comment Triage Gate" since the 2026-04-26 lesson, but each time it has been treated as advisory rather than as a hard gate, and each time green-CI was read as terminal. The pattern is structural: a checklist that lives inside Phase 9 prose, however carefully worded, gets skipped in practice if the next step doesn't physically surface.
- **Correction:** Two fixes. (1) Folded the CodeRabbit finding into PR #511 (commit 05cedade) — coerce with `typeof === number && Number.isFinite && >= 1`. (2) Restructured the development-workflow skill: added a `<HARD-GATE>` block at the top of Phase 9 explicitly stating "Green CI is not Done," promoted 9d to "MANDATORY — no early exit" with its own HARD-GATE that requires running both queue-refresh AND direct `gh api` fetches AND printing the result in the transcript, and rewrote 9e's Done criteria to require the triage transcript be visible *in the current 9a–9e execution window* before announcing "ready for merge." Added the failure mode to the autonomy guidelines ("Things you may NEVER autonomously skip"). Updated the Quick Reference table to list 9a–9e as separate rows with their own skip-conditions. The 2026-05-16 correction asked for the triage commands to be physical checklist items in `progress.md`; this round adds a hard structural gate to the skill itself so the prose can't be skimmed past.
- **Rule:** "CI green" and "ready to merge" are two different states. The bridge between them is reading every reviewer's actual comment text — not status checks, not summary fields. On every PR, even ones where the queue refresh says zero open items: run `gh api repos/$REPO/pulls/$PR/comments` AND `gh api repos/$REPO/issues/$PR/comments` AND `gh pr view $PR --json reviews`, print all three to the terminal, and act on every non-empty row (fix-with-commit, reply-with-reason, or read-and-skip-as-nit). The phrase "ready for merge" is not allowed in a notify-the-user message until that triage transcript exists in the current execution window. If this lesson surfaces a fourth time, the next correction is mechanical, not advisory — fail-closed tooling that refuses to print "Done" until the three fetches were run against the latest commit hash.

### [2026-05-23] Asking an LLM to compute calendar dates from a bare anchor is a stable, repeatable bug
- **Mistake:** The AI schedule generator prompt anchored the week with `## Target Week\nWeek starting: 2026-06-08`. The model was expected to derive each named day's date from that one line. In prod this consistently shifted day-name → date by +1 day: weekend-only templates (`days = [0, 5, 6]` Sun/Fri/Sat) landed on Mon/Sat/Sun, and week templates landed Tue–Fri. The `DAY_NOT_IN_TEMPLATE` validator (PR #511) dropped every mis-dated shift, so the failure surfaced as ~151h of under-fill (Mon = 0/X, Fri = 0/X in the planner) rather than as wrong data — masking the cause behind a symptom that looked like missing employees. Diagnosing it required reading the actual JSON the LLM emitted plus comparing against `template.days`; the bare anchor format had been in place since the initial scheduler prompt and the bug was latent for every prior schedule.
- **Correction:** Replace the bare anchor with an explicit 7-row day-name → date map (Monday-first, labels right-padded to 9 chars so the date column aligns) AND inline the matching date on every Required Headcount Per Slot line. The model now reads a literal table, not prose; no calendar math required. PR #516 — `_shared/schedule-prompt-builder.ts` + tests. Helper is UTC-anchored (parse `${weekStart}T00:00:00Z`, add ms in UTC, read UTC accessors) so prompts are byte-identical across CI/prod/dev. Validator stays as the second-layer net.
- **Rule:** Any time an LLM is asked to map between two representations (day name ↔ date, IDs ↔ names, codes ↔ descriptions), emit the mapping in the prompt as a literal table — not as an anchor the model has to expand. Cost is small (a few hundred tokens), payoff is that drift becomes a prompt-text-test problem rather than an emergent runtime problem. When the validator already catches the symptom, treat the under-fill / drop rate itself as a signal that the prompt is asking the model to do work it shouldn't have to.

### [2026-05-23] When two reviewers flag the same minor, fold it — even if CLAUDE.md says "don't validate internal calls"
- **Mistake:** Phase 7a maintainability reviewer and Phase 9b CodeRabbit independently flagged the same defensive guard: in the Required Headcount loop, an out-of-range `day` index would emit `"Day 7 undefined: 3"` into the prompt because `weekDates.byDayOfWeek[day]` returns `undefined` and gets string-interpolated. The CLAUDE.md rule says "Don't add error handling, fallbacks, or validation for scenarios that can't happen. Trust internal code and framework guarantees." `template.days` is constrained 0..6 upstream, so by the strict reading of the rule, no guard is needed. I initially deferred the maintainability minor on that basis. CodeRabbit then flagged the same thing — and the existing code already had a `DAY_NAMES[day] ?? \`Day ${day}\`` fallback for the name side, so the file's own precedent said "we guard this lookup."
- **Correction:** Folded. Replaced both lookups with a symmetric pattern that skips the entry if either name or date is missing (preferable to the old "Day 7" fallback, which would have emitted a corrupt row instead of silently dropping it). Added a unit test that locks the skip behaviour against a stray `day=7` requiredStaff entry. CodeRabbit acknowledged the symmetric guard as "a stronger fix than what I originally suggested."
- **Rule:** CLAUDE.md "don't validate internal calls" is a default, not an absolute. When (a) two independent reviewers flag the same defensive gap, AND (b) the file already has a sibling guard for the same shape of lookup, the consistency argument outranks the trust-internal-code default. Fold the guard, write a test that exercises it, and move on — the cost is a few lines and the payoff is no future surprise when "constrained upstream" turns out to mean "constrained until a migration adds a nullable column."

### [2026-05-24] Sonar "Coverage on New Code" counts conditions, not lines — multi-branch ternaries silently fail the gate
- **Mistake:** PR #520 added a success-toast that composed strings via nested ternaries and `if (count > 0)` guards: `required > 0 ? "X of Y slots" : "X shift${filled === 1 ? '' : 's'} created"` plus three more `if (... > 0)` branches plus a `budget_variance_pct > 0` suffix. Existing tests covered the `required > 0` path and the plural-shifts path. `new_uncovered_lines` was 0, so I assumed coverage was fine. SonarCloud failed the PR with `77.3% Coverage on New Code (required ≥ 80%)`. The metric I missed: `new_uncovered_conditions = 5`. Each `?:` and each `if` is a separate branch in Sonar's accounting — and adding 5 new conditions onto a single toast composer drops below 80% the moment any one branch is untested.
- **Correction:** Added one focused test that exercised four branches in a single mutation: `filled === 1` singular form, `applied_swaps_count > 0` with singular form (count=1), `rejected_swaps_count > 0` (count=2), and `budget_variance_pct > 0` (12.4%). All four assertions on one toast description. SonarCloud went 77.3% → ≥80% on the next run. `new_uncovered_conditions` dropped from 5 to 0.
- **Rule:** Whenever a diff adds branching to user-facing string composition (toasts, descriptions, labels) — count the branches before pushing. For `n` new ternaries / guards on the same statement, you need at least `2n` test assertions to keep the new-code coverage above 80%. Prefer a single test that constructs a fixture exercising all branches simultaneously (multiple `if` flags + a non-default ternary) over `n` separate tests — Sonar credits branch coverage from any test, and one fixture is far cheaper to maintain.

### [2026-05-24] Pre-validation guardrails miss post-validation drops — fire the guardrail on the response payload, not the pipeline input
- **Mistake:** The new code-first solver pipeline was `solveSchedule → applyPreferences → validateGeneratedShifts`. The zero-shift 422 guardrail checked `finalShifts.length === 0` (the output of `applyPreferences`, BEFORE the validator ran). But the success response shipped `shifts: validShifts` (AFTER the validator). If the solver emitted N shifts and the validator dropped every one — e.g. position/availability/locked-conflict drops — the guardrail would not fire and the client would receive HTTP 200 with `shifts: []` plus a metadata object claiming `total_generated = N, total_valid = 0`. That violates the 422 diagnostic contract clients rely on to distinguish "AI returned nothing" from "AI returned plenty, all invalid." Codex P1 caught it on the open PR; no other reviewer flagged it.
- **Correction:** Changed the guardrail predicate to `validShifts.length === 0`. The diagnostic body still reports `total_generated = finalShifts.length` separately so the client gets both numbers when the validator zeroes things out. Added a comment naming the invariant: "Check validShifts (post-validation), not finalShifts. If the solver emits N shifts but the validator drops every one, we'd otherwise return HTTP 200 with shifts: [] — bypassing the 422 diagnostic contract."
- **Rule:** When a pipeline has `produce → mutate → validate → respond`, the response-shape guardrails MUST run against the same data that becomes the response body. Treat the pipeline's final stage as the only authoritative source for any `length === 0` / "did we fail" check. The intermediate counts are diagnostic, not gating. When reviewing a guardrail PR, grep for the variable used in the response payload and confirm the guardrail checks the same variable.

### [2026-05-24] Solver-computed metadata goes stale when a post-solver layer mutates assignments — recompute, don't reuse
- **Mistake:** `solveSchedule` computes a `fairness` summary inline as it assigns shifts: per-employee hours_assigned + days_worked. The new preference-LLM layer (`applyPreferences`) can swap shifts between employees AFTER the solver runs. The edge function shipped `fairness_summary: solverResult.fairness` directly — meaning a cross-day or cross-length swap (e.g. swap a 6.5h Mon shift between employees A and B with a 12.5h Wed shift) left both rows with the wrong hours_assigned/days_worked attributed to the wrong employee. The UI consumed it as the source of truth for "this person got X hours" and would show whatever the solver intended pre-swap, not what the user actually got.
- **Correction:** Extracted `computeFairness(shifts, employees)` as a pure helper in `schedule-solver.ts`. The edge function now calls it on `validShifts` (post-swap, post-validation) instead of using `solverResult.fairness`. Added three unit tests: per-employee sum + distinct-day count, zero-fill for employees with no shifts, and a swap-reflection test that asserts hours flip correctly when only `employee_id` mappings change. Solver's pre-swap fairness is still produced for internal observability but is no longer the response source.
- **Rule:** Any metadata produced inline during a pipeline stage becomes a candidate for staleness the moment a downstream stage can mutate the underlying data. The fix is either (a) compute metadata only at the end, on the final dataset, or (b) extract the metadata producer as a pure helper and re-run it on the final dataset. Never ship metadata that was computed before a mutation that could affect it. Test it with a fixture that does exactly that mutation and asserts the metadata changes accordingly.

### [2026-05-24] `${arr[-1]}` needs Bash 4.3+ — macOS still ships Bash 3.2 by default
- **Mistake:** `scripts/verify-schedule-perf.sh` used `${sorted[-1]}` to grab the max element of a sorted array (the project standard CI runner is Linux, which has modern Bash, so the script passed locally and in CI). CodeRabbit's analysis script ran on a macOS-like environment and flagged that Bash 3.2 (Apple's default — they haven't updated since the GPLv3 license change) does not support negative array indexing. Anyone running the perf verify script on a stock Mac terminal without Homebrew Bash gets `${sorted[-1]: bad array subscript`.
- **Correction:** Replaced `${sorted[-1]}` with `${sorted[${#sorted[@]}-1]}` (compute the last-index explicitly). Added an inline comment naming the constraint so the next person doesn't "simplify" it back. Also added `curl --fail-with-body` (CR #4) to the same script so that 4xx/5xx responses don't get measured as successful runs.
- **Rule:** Any shell script intended to be run by developers (not just CI) must work on Bash 3.2. Avoid `${arr[-1]}`, `${var,,}`, `${var^^}`, `mapfile`/`readarray`, associative arrays. When in doubt, run `bash --version` on a stock macOS terminal — `3.2.57` is the marker. If a script genuinely needs Bash 4+, gate it with `[[ ${BASH_VERSINFO[0]} -lt 4 ]] && { echo "Bash 4+ required"; exit 1; }` at the top so the failure is loud and explained.

### [2026-05-26] Edge-function SELECT projections must be tested against a real schema, not just against TS mocks
- **Mistake:** `ai-execute-tool/index.ts` defined `EMPLOYEE_LABOR_COLUMNS` with `compensation_history` listed as a bare column on `employees`. PostgreSQL returned `column employees.compensation_history does not exist`, the edge function bubbled the error as HTTP 500, and the AI tool router surfaced it to managers as `TOOL_ERROR` for every labor-cost or time-punch query. The canonical working pattern lived 20 files away in `useEmployees.tsx`: PostgREST's embed-alias form `compensation_history:employee_compensation_history(...)` that follows the FK and returns the rate-history rows as a nested array. The wrong projection shipped in PR #519, sat in prod, and was only caught by an end-user complaint — every unit test for `laborCalculations` constructed `Employee` mocks directly with `compensation_history: []` pre-populated, so the projection layer never ran in CI. `useEmployees` had the right shape, so `src/`-side typecheck never disagreed with the wrong edge-function string. Phase 7a reviewers (security/perf/maintainability/sound-logic) and CodeRabbit read code semantics, not SELECT strings against schema. No E2E smoke test invoked the AI tool against a live DB with a real JWT.
- **Correction:** Switched to the embed-alias form, narrowed from `(*)` to the four columns `resolveCompensationForDate` actually reads, and extracted the projection to `supabase/functions/_shared/employeeLaborColumns.ts` so it's importable from Vitest (Deno `https://` imports in the edge entry blocked direct test import). Added two regression layers: `tests/unit/employeeLaborColumns.test.ts` (string-shape contract — embed-alias present, no bare token, embed columns match consumed fields, base-column tokens snapshot against the actual `employees` schema), and `supabase/tests/employee_compensation_history_embed.test.sql` (pgTAP `has_table` + `fk_ok` pinning the FK PostgREST follows for the embed — catches wrong-table embeds string-shape can't). Both run on every PR.
- **Rule:** Any SELECT projection string built in an edge function (or anywhere outside a generated client) must be either (a) covered by a unit test that asserts the string shape against a hard-coded snapshot of the columns that actually exist in the migrations, AND (b) covered by a pgTAP test that proves any embedded relations resolve via real FKs — or (c) generated from `mcp__supabase__generate_typescript_types` and consumed type-safely. Bare strings to PostgREST without either guard are how phantom-column TOOL_ERRORs ship to prod. The string-shape test belongs in `tests/unit/`, the FK assertion in `supabase/tests/`, and the projection itself in a pure `_shared/` module (no Deno imports) so Vitest can import it.

---

## Category: Testing (Vite/jsdom) (continued)

### [2026-05-26] Node 20 webcrypto rejects cross-realm ArrayBuffers from jsdom's `Blob.arrayBuffer()`
- **Mistake:** PR #523 (`receipt-duplicate-warning`) added `src/lib/fileHash.ts` with `crypto.subtle.digest('SHA-256', await blob.arrayBuffer())`. Locally on Node 24 the unit tests passed (15/15) and the full suite passed (4180/4181) cleanly — declared "Phase 6 green" and pushed. CI failed: `TypeError: Failed to execute 'digest' on 'SubtleCrypto': 2nd argument is not instance of ArrayBuffer, Buffer, TypedArray, or DataView.` Root cause: Node 20's webcrypto (still the CI matrix in `.github/workflows/unit-tests.yml`) enforces a strict cross-realm `ArrayBuffer` identity check on the `BufferSource` parameter, and the buffer returned by jsdom's `Blob.arrayBuffer()` lives in jsdom's realm — not the V8 main realm webcrypto expects. Node 24 relaxed that check, so the test never failed locally. Cost was one round-trip CI cycle plus diagnosis time installing Node 20.20.2 via nvm to reproduce.
- **Correction:** Byte-copy into a fresh `Uint8Array` in the current realm before digesting: `const buffer = await blob.arrayBuffer(); const bytes = new Uint8Array(buffer.byteLength); bytes.set(new Uint8Array(buffer)); const digest = await crypto.subtle.digest('SHA-256', bytes);`. Verified the fix on Node 20.20.2 (15/15 target tests + 4180/4181 full suite) before re-pushing (`abf8730b`). CI flipped green.
- **Rule:** When using `crypto.subtle.digest()` (or any webcrypto API) with bytes that originated from a foreign realm — `Blob.arrayBuffer()` under jsdom, structured-cloned data from a worker, anything returned by a third-party browser-shim library — always normalize through a fresh same-realm `Uint8Array(byteLength)` + `.set()` before passing to the crypto API. Belt-and-suspenders verification: read `.github/workflows/*.yml` and confirm the local Node version matches the CI matrix before declaring tests green locally. The `node-version` line is one grep away and saves the round-trip when subtle realm/feature differences exist between major versions.

---

## Category: UI Patterns (continued)

### [2026-05-26] `<input type="file">` must reset its value to support cancel-then-retry flows
- **Mistake:** PR #523's `ReceiptUpload.tsx` triggered the upload via `<input type="file" onChange={handleFileUpload}>`. The flow was: user picks file → byte-hash detects existing upload → `DuplicateReceiptDialog` opens → user clicks Cancel. Re-selecting the *same* file from the picker silently no-op'd. Native `<input type="file">` only fires `onChange` when the selected value changes; clicking Cancel on the duplicate dialog left the input's `.value` set to the same filename, so the next selection of the identical file produced no event. The bug was discovered only during manual QA in Phase 5 — every automated test passed because they synthesized the change event directly via `fireEvent.change(input, { target: { files } })`, which bypasses the browser's value-change gate.
- **Correction:** Captured `const input = event.currentTarget` synchronously (before any `await`, since React pools the event), ran the upload in a `try`, and reset `input.value = ''` in `finally`. This forces the next selection — including re-selecting the same file — to fire `onChange`. Pattern lives in `handleFileUpload`, not in the duplicate-dialog cancel handler, so it works for any future "process and bail" branch (rejection, network error, validation failure), not just the duplicate path.
- **Rule:** Any `<input type="file">` that feeds into a conditional confirm/cancel/error flow must reset its `.value` to `''` in `finally` after processing. Capture `event.currentTarget` into a local before any `await` because React synthetic events get nulled out across yield points. Synthesized `fireEvent.change` in tests bypasses the browser's value-equality gate, so this bug class is invisible to standard unit tests — verify the cancel-then-retry-same-file path manually in a real browser, or write a Playwright E2E for it. Test-only coverage is functionally blind to this category of native-element behavior.

---

## Category: CI / Workflows (continued)

### [2026-05-26] GitHub fires `push` + `pull_request` concurrently on PR commits — Supabase preview branch races on `schema_migrations_pkey`
- **Mistake:** PR #523's second CI run (after the Node 20 fix push) reported a wave of pgTAP and E2E failures with `ERROR: duplicate key value violates unique constraint "schema_migrations_pkey" (SQLSTATE 23505) Key (version)=(20260524120000) already exists.` Initial reaction was to assume a real migration conflict. Cross-checked the Actions tab: GitHub had fired BOTH a `push` event (for `feature/receipt-duplicate-warning`) AND a `pull_request` event (for PR #523) on the exact same commit SHA, triggering two parallel workflow runs. Both runs called `supabase db push` against the same shared Supabase preview branch, racing on the migrations table. Whichever instance committed second got the unique-key violation, and every job depending on that DB instance cascaded to red. The push-triggered run had finished green minutes earlier — the visible "failures" came entirely from the slower duplicate run. Without recognizing the pattern, this looks identical to "your migration breaks production" and would have wasted hours of code review.
- **Correction:** Diagnosed via the Actions UI showing two runs on the same SHA with different `event` types; confirmed the push run's gates were all green and the pull_request run's failures were infrastructure conflicts, not code regressions. Documented in `progress.md` so PR triage knew which run to trust. Long-term fix (not in this PR): add a `concurrency:` block to the workflow with `group: ${{ github.workflow }}-${{ github.ref }}` and `cancel-in-progress: true` to deduplicate at the GitHub layer, OR add an `if: github.event_name == 'pull_request'` guard on the `push` job to suppress one event entirely on PR commits.
- **Rule:** When CI shows two concurrent runs on the same commit SHA, check `gh run list --json event,headSha,conclusion` and identify which event triggered each. If the failures are all DB-init-time (`schema_migrations_pkey`, `relation already exists`, `tuple concurrently updated`) and one of the two runs is fully green, the failures are concurrent-run race conditions against shared infra — NOT code regressions. Don't fix code for an infra bug. Cross-reference with the trigger config in `.github/workflows/*.yml`: any workflow with both `on: push: branches:` AND `on: pull_request:` will double-fire on PR commits unless explicitly deduped. Add `concurrency:` with `cancel-in-progress` as the permanent fix; the user notification when a PR is "green" must specify which event's run is the canonical one to trust.

---

## Category: Accessibility

### [2026-05-29] Radix dialog subtitle must be `<DialogDescription>`, not a plain `<p>` — and the CLAUDE.md pattern seeded the bug
- **Mistake:** During Phase 5 UI review of PR #526 (`reactivate-pin-checkbox`, commit `9b81171a`), the dialog header was restyled to the CLAUDE.md "Dialog Structure" Apple/Notion pattern, which rendered the subtitle as `<p className="text-[13px] text-muted-foreground mt-0.5">Reactivate {name}</p>`. That replaced the Radix `<DialogDescription>`, so Radix stopped wiring `aria-describedby` on the dialog root and screen readers lost the dialog's descriptive context. The Codex reviewer (`chatgpt-codex-connector`) flagged it P2 in Phase 9d; fixed in `921f059f` by swapping the `<p>` back to `<DialogDescription>` (same className). Root cause: the **documented CLAUDE.md pattern itself** showed a plain `<p>` for the description, so following the house style verbatim produced the regression.
- **Correction:** Use `<DialogDescription className="...">` for the subtitle. Also fixed the CLAUDE.md "Dialog Structure" snippet to use `<DialogDescription>` (with a comment) so the documented pattern no longer seeds the bug.
- **Rule:** In any Radix/shadcn `Dialog`, the title is `<DialogTitle>` and the descriptive subtitle is `<DialogDescription>` — apply visual styling via `className` on those primitives, never substitute a plain `<p>`/`<div>` to chase a spec. Radix derives `aria-labelledby`/`aria-describedby` from their presence. If a dialog truly has no description, pass `aria-describedby={undefined}` to `<DialogContent>` intentionally rather than dropping the element silently. When copying a documented styling pattern, check it still uses the semantic primitive — a pretty snippet can be an a11y trap.

---

## Category: JavaScript / String handling

### [2026-05-29] `String.replace('_', ' ')` only replaces the FIRST occurrence
- **Mistake:** `ReactivateEmployeeDialog` humanized a status slug with `employee.deactivation_reason.replace('_', ' ')`. With a string (not regex) first argument, `String.prototype.replace` replaces only the first match, so a multi-word slug like `on_unpaid_leave` rendered as "on unpaid_leave". Pre-existing in the dialog; a Phase 7 multi-model reviewer caught it while the file was open (fixed in `a768851b`).
- **Correction:** Use a global regex: `.replace(/_/g, ' ')`.
- **Rule:** When humanizing `snake_case`/`kebab-case` slugs for display, always use a global regex (`/_/g`, `/-/g`) — the bare-string form silently stops after the first occurrence. Grep for `.replace('_'`, `.replace("_"`, `.replace('-'` when touching any slug-to-label code.

---

## Category: SonarCloud / Coverage (continued)

### [2026-05-29] Type-only test files: use `export {}` to mark a module, not `void x` to suppress unused vars
- **Mistake:** The type-level regression guard `src/types/__tests__/reactivate-params.type-test.ts` (a `@ts-expect-error` assignment proving `ReactivateEmployeeParams` rejects `confirmPin`) used `void _typeGuard;` to silence the unused-variable lint. SonarCloud flagged `typescript:S3735` "Remove this use of the void operator" — the single OPEN item in the PR #526 review queue. A second, duplicate `@ts-expect-error` block also existed inline in `employeeActivation.test.ts`, splitting the same assertion across two files.
- **Correction:** Replaced `void _typeGuard;` with `export type { };` (an empty export makes the file an ES module, which suppresses the unused-local warning without the void operator) in `cd7ebce8`. Removed the duplicate inline block, keeping one canonical type-test file (`a768851b`).

---

## Category: CI / Workflows (continued)

### [2026-05-31] dev-build-and-ship orchestrator: four ship-time hardening gaps

- **Mistake:** Across PRs #525, #529, and #530 the autonomous orchestrator (`.claude/workflows/dev-build-and-ship.js`) repeated four classes of failure. (1) **Ephemeral-artifact commits** — agents used blanket staging, dragging gitignored `progress.md` and `dev-tools/9d-triage-*` (and once a *prior* branch's triage log, leaking machine/auth metadata) into feature PRs. (2) **9d triage under-reported severity** — the triage agent returned "all triaged / N informational" while real CodeRabbit 🟠 Major and Codex P1 comments sat unaddressed (twice on #529). (3) **Branch-slash filename mangling** — branch `fix/foo` turned `9d-triage-<branch>.md` into a `fix/` subdirectory. (4) **Out-of-order migrations** — a timestamp picked at creation time relative to then-current main was leapfrogged when a parallel branch merged first, so `supabase db push` rejected the older timestamp and the prod deploy failed for two merges.
- **Correction:** (1) `envelope()` now injects a COMMIT HYGIENE block into every agent prompt: never `git add -A`/`.`/`--all`, stage explicit paths only, and run `git restore --staged progress.md dev-tools/` + a `git diff --cached --name-only` check before every commit; the 9d triage artifact is written to the gitignored `dev-tools/` path and left untracked. (2) The Phase 9d prompt records each comment's OWN badge verbatim (CodeRabbit critical/major/minor/nitpick; Codex P1/P2/P3), forbids collapsing Major/P1 to "informational", and surfaces `actionableCommentCount` parsed from CodeRabbit's "Actionable comments posted: N"; the schema adds `severityBadges` + counts, and script-level gates block when `triagedCount < actionableCommentCount` or `openCriticalOrMajor > 0`. (3) A `sanitizeBranch()` helper (`/`→`-`) feeds `SAFE_BRANCH`, used for every branch-name-in-path. (4) Phase 9a, before push, fetches `origin/main`, computes the max on-main migration timestamp, and renumbers (via `git mv`, explicit-path commit) only branch-new migrations whose timestamp ≤ that max — never touching migrations already on main. `--include-all` in `deploy-supabase.yml` (#530) remains the runtime net.
- **Rule:** In any agent-driven workflow/orchestrator: (a) forbid glob staging — explicit `git add <path>` plus a staged-file pre-check before every commit, and write ephemeral artifacts only to gitignored paths; (b) sanitize branch names (`/`→`-`) before embedding them in any filesystem path; (c) preserve each bot's own severity badge verbatim and parse its declared actionable-comment count, then gate "done" on both no-open-Major/P1 and triaged ≥ actionable so nothing is silently downgraded; (d) renumber branch-new migrations against `origin/main` at ship time so out-of-order timestamps never reach prod.
