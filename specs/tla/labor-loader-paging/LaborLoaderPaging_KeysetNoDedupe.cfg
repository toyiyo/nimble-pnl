\* EXPECT: violation. Counterfactual: keyset paging with no id de-duplication. An edit moves a row that the loader read to a key after the cursor, so the loader receives the row again.
SPECIFICATION Spec
CONSTANTS
    Keys = {1, 2, 3, 4, 5, 6}
    InitRows = {2, 3, 4, 5}
    PageSize = 2
    MaxWrites = 2
    UseKeyset = TRUE
    Dedupe = FALSE
INVARIANTS
    TypeOK
    NoDuplicateRow
    NoLostStableRow
    StableRowCurrentKey
