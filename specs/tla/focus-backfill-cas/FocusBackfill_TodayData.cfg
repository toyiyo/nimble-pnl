\* Today (timeout 5 s): two workers per tick. The design claims the CAS keeps the data correct.
SPECIFICATION Spec
CONSTANTS
    Workers = {"w1", "w2"}
    TargetDays = 3
    MaxDays = 2
    Runs = 2
    UseCas = TRUE
INVARIANTS
    NoGap
    DoneMeansAllDays
    ErrorPointsAtCursor
PROPERTIES
    CursorMonotonic
CHECK_DEADLOCK FALSE
