\* Design: keyset paging on (punch_time, id), with one copy of each id kept.
SPECIFICATION Spec
CONSTANTS
    Keys = {1, 2, 3, 4, 5, 6}
    InitRows = {2, 3, 4, 5}
    PageSize = 2
    MaxWrites = 2
    UseKeyset = TRUE
    Dedupe = TRUE
INVARIANTS
    TypeOK
    NoDuplicateRow
    NoLostStableRow
    StableRowCurrentKey
