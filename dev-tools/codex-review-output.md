::finding:: severity=critical file=src/components/TeamMembers.tsx line=237
`operations_manager` can self-promote to full `manager`: line 55 enables the team-member role editor for `operations_manager`, and the role dropdown always includes `<SelectItem value="manager">`. When the ops manager opens their own row and selects Manager, `updateMemberRole()` writes directly to `user_restaurants`; the existing RLS policy permits users to update their own association (`user_id = auth.uid()` in both USING/WITH CHECK), so this is not blocked server-side. That grants the accounting/admin-capable `manager` role, violating the role’s “no accounting or admin” boundary.
