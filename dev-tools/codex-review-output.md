::finding:: severity=major file=src/components/pos-sales/SalesTrendsPanel.tsx line=63
`posFilter` is never reconciled when a refetched range has a different `data.pos_systems` set. Trigger: select `square` in a range with Toast + Square, then change the page dates to a range containing only Toast sales. Line 63 filters the new payload by stale `square`, producing zero KPIs/charts, while line 99 hides the POS control because the new range has only one POS, leaving the user unable to switch back to “All POS” and showing false empty trends despite real Toast sales in the range.
