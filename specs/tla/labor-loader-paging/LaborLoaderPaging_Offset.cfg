\* EXPECT: violation. Counterfactual: offset paging as fetchAllRows does now. One concurrent write between pages duplicates or drops a row.
SPECIFICATION Spec
CONSTANTS
    Keys = {1, 2, 3, 4, 5, 6}
    InitRows = {2, 3, 4, 5}
    PageSize = 2
    MaxWrites = 1
    UseKeyset = FALSE
INVARIANTS
    TypeOK
    NoDuplicateRow
    NoLostStableRow
