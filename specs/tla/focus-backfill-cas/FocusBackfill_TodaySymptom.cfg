\* EXPECT: violation. Today (timeout 5 s): TLC must reproduce the HTTP 400 banner.
SPECIFICATION Spec
CONSTANTS
    Workers = {"w1", "w2"}
    TargetDays = 3
    MaxDays = 2
    Runs = 2
    UseCas = TRUE
INVARIANTS
    NoConflictBanner
CHECK_DEADLOCK FALSE
