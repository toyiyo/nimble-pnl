::finding:: severity=major file=supabase/migrations/20251011012229_edd566c1-3d02-4ae0-98f7-6363d034b1bc.sql line=21
`trigger_square_periodic_sync()` still hardcodes the production `square-periodic-sync` URL and is a `SECURITY DEFINER` RPC with no later `REVOKE` found, so it keeps the default `PUBLIC` execute grant. Trigger: any preview/local DB or client role that can call `public.trigger_square_periodic_sync()` bypasses the new `deploy_env`/`cron_invoke_edge` guard entirely and enqueues a prod Square sync from line 21, leaving a non-cron preview-to-prod invocation path after this migration claims to centralize prod edge dispatch.
