\* After the fix (timeout 120 s): one worker per tick. Data safety and no 400 banner.
SPECIFICATION Spec
CONSTANTS
    Workers = {"w1"}
    TargetDays = 3
    MaxDays = 2
    Runs = 3
    UseCas = TRUE
INVARIANTS
    NoGap
    DoneMeansAllDays
    ErrorPointsAtCursor
    NoConflictBanner
PROPERTIES
    CursorMonotonic
CHECK_DEADLOCK FALSE
