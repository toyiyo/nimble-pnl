::finding:: severity=major file=supabase/migrations/20260802120000_check_printing_capability_gating.sql line=323
The new `check_bank_accounts` SELECT policy grants every `view:banking` / `books@view` role direct table read access, but this table includes `routing_number`, `account_number_last4`, and `account_number_encrypted`. A read-only books custom role that is correctly excluded from `/print-checks` and the manage-tier secrets RPC can still query Supabase directly for `check_bank_accounts` and retrieve the MICR metadata/encrypted account blob for the restaurant. The trigger/RPC hardening only protects writes and plaintext decryption; RLS should not expose this table at `view:banking` unless secret-bearing columns are split or hidden behind a safer view.
