// Moved to supabase/functions/_shared/labor/fetchAllRows.ts so the edge functions
// share one copy. This shim keeps the old import path.
export * from '../../supabase/functions/_shared/labor/fetchAllRows';
