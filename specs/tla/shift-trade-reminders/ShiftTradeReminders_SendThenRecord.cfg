\* EXPECT: violation. Counterfactual: send first, record the dedupe row after (bank-reauth order). TLC must find a double send.
SPECIFICATION Spec
CONSTANTS
    Runs = {"r1", "r2"}
    ClaimFirst = FALSE
INVARIANTS
    TypeOK
    AtMostOneSendPerStage
    NoClaimForClosedTrade
\* A run that finds the trade closed stops. That end state is valid, not a deadlock.
CHECK_DEADLOCK FALSE
