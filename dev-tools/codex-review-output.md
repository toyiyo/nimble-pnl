::finding:: severity=minor file=src/pages/Expenses.tsx line=92
The new gate only hides `PrintCheckButton` inside each pending-outflow card, but the Expenses page still renders an unconditional “Print Checks” header action that navigates to `/print-checks`. A custom role with `books@view` can access `/expenses` but is intentionally excluded from `/print-checks` (`books@manage`), so clicking this button sends them into a protected route they cannot open instead of hiding the check-printing affordance consistently.
