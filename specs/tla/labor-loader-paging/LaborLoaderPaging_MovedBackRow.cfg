\* EXPECT: violation. Accepted limit: keyset paging with de-duplication. An edit moves a row back before the cursor, so the loader does not read the row with its current key.
SPECIFICATION Spec
CONSTANTS
    Keys = {1, 2, 3, 4, 5, 6}
    InitRows = {2, 3, 4, 5}
    PageSize = 2
    MaxWrites = 2
    UseKeyset = TRUE
    Dedupe = TRUE
INVARIANTS
    TypeOK
    AllRowsCurrentKey
