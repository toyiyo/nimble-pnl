\* EXPECT: violation. Counterfactual: delete .eq('sync_cursor', readCursor). The cursor must move back.
SPECIFICATION Spec
CONSTANTS
    Workers = {"w1", "w2"}
    TargetDays = 3
    MaxDays = 2
    Runs = 2
    UseCas = FALSE
PROPERTIES
    CursorMonotonic
CHECK_DEADLOCK FALSE
